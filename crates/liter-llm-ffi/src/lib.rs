// This file is auto-generated by alef. DO NOT EDIT.
// alef:hash:c46a7dc8103b11e3b911aeb9ae3c2c36e3776537cb7b1e5484e511e73ac92a7c
// alef:handle-abi:1
// Re-generate with: alef generate
#![allow(dead_code, unused_imports, unused_mut, noop_method_call)]
#![allow(unsafe_op_in_unsafe_fn, unsafe_attr_outside_unsafe)]
#![allow(
    clippy::let_unit_value,
    clippy::needless_borrow,
    clippy::redundant_locals,
    clippy::unused_unit,
    clippy::unwrap_or_default,
    clippy::derivable_impls,
    clippy::needless_borrows_for_generic_args,
    clippy::unnecessary_fallible_conversions,
    clippy::type_complexity,
    clippy::clone_on_copy,
    clippy::collapsible_if
)]
#![allow(
    clippy::missing_safety_doc,
    clippy::doc_lazy_continuation,
    clippy::doc_overindented_list_items
)]
#![allow(clippy::single_match, clippy::collapsible_match)]

use liter_llm::client::BatchClient;
use liter_llm::client::BatchRetriever;
use liter_llm::client::FileClient;
use liter_llm::client::LlmClient;
use liter_llm::client::ResponseClient;
use std::cell::RefCell;
use std::ffi::{CStr, CString, c_char};

thread_local! {
    static LAST_ERROR_CODE: RefCell<i32> = const { RefCell::new(0) };
    static LAST_ERROR_CONTEXT: RefCell<Option<CString>> = const { RefCell::new(None) };
    static LAST_RETURN_LENGTHS: RefCell<std::collections::BTreeMap<&'static str, usize>> =
        const { RefCell::new(std::collections::BTreeMap::new()) };
}

#[repr(i32)]
pub enum AlefFfiErrorCode {
    LiterllmAlefNone = 0,
    LiterllmAlefConversion = 1,
    LiterllmAlefUnknown = 2,
    LiterllmAlefPanic = 3,
    LiterllmAlefInvalidHandle = 4,
}

const ALEF_FFI_CONVERSION_ERROR: i32 = AlefFfiErrorCode::LiterllmAlefConversion as i32;
const ALEF_FFI_UNKNOWN_ERROR: i32 = AlefFfiErrorCode::LiterllmAlefUnknown as i32;
const ALEF_FFI_PANIC_ERROR: i32 = AlefFfiErrorCode::LiterllmAlefPanic as i32;
const ALEF_INVALID_HANDLE_ERROR: i32 = AlefFfiErrorCode::LiterllmAlefInvalidHandle as i32;

fn alef_ffi_error_code(error: &dyn std::any::Any) -> i32 {
    if let Some(error) = error.downcast_ref::<liter_llm::cost::refresh::CatalogRefreshError>() {
        #[allow(unreachable_patterns)]
        return match error {
            liter_llm::cost::refresh::CatalogRefreshError::Disabled => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::cost::refresh::CatalogRefreshError::InsecureUrl { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::cost::refresh::CatalogRefreshError::Fetch { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::cost::refresh::CatalogRefreshError::Parse { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::cost::refresh::CatalogRefreshError::Cache { .. } => ALEF_FFI_UNKNOWN_ERROR,
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    if let Some(error) = error.downcast_ref::<liter_llm::error::LiterLlmError>() {
        #[allow(unreachable_patterns)]
        return match error {
            liter_llm::error::LiterLlmError::Authentication { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::RateLimited { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::BadRequest { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::ContextWindowExceeded { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::ContentPolicy { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::NotFound { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::ServerError { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::ServiceUnavailable { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::Timeout => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::Streaming { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::EndpointNotSupported { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::InvalidHeader { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::Serialization(..) => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::BudgetExceeded { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::HookRejected { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::InternalError { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::OutboundForbidden { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::IdempotencyConflict { .. } => ALEF_FFI_UNKNOWN_ERROR,
            liter_llm::error::LiterLlmError::IdempotencyInFlight { .. } => ALEF_FFI_UNKNOWN_ERROR,
            _ => ALEF_FFI_UNKNOWN_ERROR,
        };
    }
    ALEF_FFI_UNKNOWN_ERROR
}

fn set_last_error(code: i32, message: &str) {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = code);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = CString::new(message).ok());
}

fn clear_last_error() {
    LAST_ERROR_CODE.with_borrow_mut(|c| *c = 0);
    LAST_ERROR_CONTEXT.with_borrow_mut(|c| *c = None);
}

fn set_panic_error() {
    set_last_error(ALEF_FFI_PANIC_ERROR, "Rust panic contained at FFI boundary");
}

fn catch_ffi_panic<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    clear_last_error();
    catch_ffi_panic_preserving_error(fallback, body)
}

fn catch_ffi_panic_preserving_error<T>(fallback: T, body: impl FnOnce() -> T) -> T {
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(body)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call). Unlike `catch_ffi_panic` (which always
            // clears first), this wrapper's whole contract is to leave that in place, so the
            // generic panic marker is stamped only when nothing more specific already is.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            fallback
        }
    }
}

fn set_last_return_len(function: &'static str, len: usize) {
    LAST_RETURN_LENGTHS.with_borrow_mut(|lengths| {
        lengths.insert(function, len);
    });
}

fn last_return_len(function: &'static str) -> usize {
    LAST_RETURN_LENGTHS.with_borrow(|lengths| lengths.get(function).copied().unwrap_or(0))
}

/// Return the last error code (0 means no error).
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// This function does not allocate and returns no owned pointer.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_last_error_code() -> i32 {
    catch_ffi_panic_preserving_error(ALEF_FFI_PANIC_ERROR, || LAST_ERROR_CODE.with_borrow(|c| *c))
}

/// Return the last error message. The pointer is borrowed and valid until the next FFI call on this thread.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// The returned pointer is borrowed from thread-local storage and must NOT be freed.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_last_error_context() -> *const c_char {
    catch_ffi_panic_preserving_error(std::ptr::null(), || {
        LAST_ERROR_CONTEXT.with_borrow(|ctx| ctx.as_ref().map_or(std::ptr::null(), |c| c.as_ptr()))
    })
}

type AlefHandle = u64;
const HANDLE_INDEX_MASK: u64 = u32::MAX as u64;

#[derive(Debug)]
enum HandleError {
    InvalidZero,
    UnknownSlot,
    StaleGeneration,
    WrongType,
    RegistryPoisoned,
    HandleBusy,
    AliasedHandle,
}

impl std::fmt::Display for HandleError {
    fn fmt(&self, formatter: &mut std::fmt::Formatter<'_>) -> std::fmt::Result {
        let message = match self {
            Self::InvalidZero => "zero is not a valid handle",
            Self::UnknownSlot => "handle refers to an unknown slot",
            Self::StaleGeneration => "handle generation is stale",
            Self::WrongType => "handle has the wrong type",
            Self::RegistryPoisoned => "handle registry lock is poisoned",
            Self::HandleBusy => "handle is currently in use",
            Self::AliasedHandle => "the same handle was passed more than once",
        };
        formatter.write_str(message)
    }
}

type ErasedHandleValue = std::sync::Arc<std::sync::Mutex<Box<dyn std::any::Any + Send>>>;

struct HandleSlot {
    generation: u32,
    value: Option<ErasedHandleValue>,
}

#[derive(Default)]
struct HandleRegistry {
    slots: Vec<HandleSlot>,
}

impl HandleRegistry {
    fn insert<T: std::any::Any + Send>(&mut self, value: T) -> AlefHandle {
        let entry = std::sync::Arc::new(std::sync::Mutex::new(Box::new(value) as Box<dyn std::any::Any + Send>));
        if let Some((index, slot)) = self.slots.iter_mut().enumerate().find(|(_, slot)| slot.value.is_none()) {
            slot.value = Some(entry);
            return encode_handle(index, slot.generation);
        }
        self.slots.push(HandleSlot {
            generation: 1,
            value: Some(entry),
        });
        encode_handle(self.slots.len() - 1, 1)
    }

    fn get(&self, handle: AlefHandle) -> Result<ErasedHandleValue, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        slot.value.clone().ok_or(HandleError::StaleGeneration)
    }

    fn remove<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<(), HandleError> {
        drop(self.take::<T>(handle)?);
        Ok(())
    }

    fn take<T: std::any::Any + Send>(&mut self, handle: AlefHandle) -> Result<T, HandleError> {
        let (index, generation) = decode_handle(handle)?;
        let slot = self.slots.get_mut(index).ok_or(HandleError::UnknownSlot)?;
        if slot.generation != generation {
            return Err(HandleError::StaleGeneration);
        }
        let value = slot.value.as_ref().ok_or(HandleError::StaleGeneration)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if !guard.is::<T>() {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        let value = slot.value.take().ok_or(HandleError::StaleGeneration)?;
        let mutex = std::sync::Arc::try_unwrap(value).map_err(|value| {
            slot.value = Some(value);
            HandleError::HandleBusy
        })?;
        slot.generation = next_generation(slot.generation);
        let boxed = mutex.into_inner().map_err(|_| HandleError::RegistryPoisoned)?;
        boxed
            .downcast::<T>()
            .map(|value| *value)
            .map_err(|_| HandleError::WrongType)
    }
}

static HANDLE_REGISTRY: std::sync::OnceLock<std::sync::Mutex<HandleRegistry>> = std::sync::OnceLock::new();

fn handle_registry() -> &'static std::sync::Mutex<HandleRegistry> {
    HANDLE_REGISTRY.get_or_init(|| std::sync::Mutex::new(HandleRegistry::default()))
}

fn insert_handle<T: std::any::Any + Send>(value: T) -> Result<AlefHandle, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    Ok(registry.insert(value))
}

struct SerializedHandle<T: 'static> {
    json: String,
    marker: std::marker::PhantomData<fn() -> T>,
}

fn insert_serialized_handle<T: serde::Serialize + 'static>(value: &T) -> Result<AlefHandle, String> {
    let json = serde_json::to_string(value).map_err(|error| error.to_string())?;
    insert_handle(SerializedHandle::<T> {
        json,
        marker: std::marker::PhantomData,
    })
    .map_err(|error| error.to_string())
}

fn with_handle<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_ref::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

struct HandleRequest {
    handle: AlefHandle,
    expected_type: std::any::TypeId,
}

fn acquire_handles(requests: &[HandleRequest]) -> Result<Vec<(AlefHandle, ErasedHandleValue)>, HandleError> {
    let mut ordered = requests.iter().collect::<Vec<_>>();
    ordered.sort_by_key(|request| request.handle);
    if ordered.windows(2).any(|pair| pair[0].handle == pair[1].handle) {
        return Err(HandleError::AliasedHandle);
    }
    let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let mut values = Vec::with_capacity(ordered.len());
    for request in ordered {
        let value = registry.get(request.handle)?;
        let guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
        if guard.as_ref().type_id() != request.expected_type {
            return Err(HandleError::WrongType);
        }
        drop(guard);
        values.push((request.handle, value));
    }
    Ok(values)
}

fn locked_handle_ptr<T: std::any::Any + Send>(
    guards: &mut [(AlefHandle, std::sync::MutexGuard<'_, Box<dyn std::any::Any + Send>>)],
    handle: AlefHandle,
) -> Result<*mut T, HandleError> {
    let (_, guard) = guards
        .iter_mut()
        .find(|(candidate, _)| *candidate == handle)
        .ok_or(HandleError::UnknownSlot)?;
    guard
        .downcast_mut::<T>()
        .map(|value| value as *mut T)
        .ok_or(HandleError::WrongType)
}

fn with_handle_mut<T: std::any::Any + Send, R>(
    handle: AlefHandle,
    operation: impl FnOnce(&mut T) -> R,
) -> Result<R, HandleError> {
    let value = {
        let registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
        registry.get(handle)?
    };
    let mut guard = value.lock().map_err(|_| HandleError::RegistryPoisoned)?;
    let typed = guard.downcast_mut::<T>().ok_or(HandleError::WrongType)?;
    Ok(operation(typed))
}

fn remove_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<(), HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.remove::<T>(handle)
}

fn take_handle<T: std::any::Any + Send>(handle: AlefHandle) -> Result<T, HandleError> {
    let mut registry = handle_registry().lock().map_err(|_| HandleError::RegistryPoisoned)?;
    registry.take::<T>(handle)
}

fn encode_handle(index: usize, generation: u32) -> AlefHandle {
    ((generation as u64) << 32) | ((index as u64) + 1)
}

fn decode_handle(handle: AlefHandle) -> Result<(usize, u32), HandleError> {
    if handle == 0 {
        return Err(HandleError::InvalidZero);
    }
    let encoded_index = handle & HANDLE_INDEX_MASK;
    if encoded_index == 0 {
        return Err(HandleError::UnknownSlot);
    }
    Ok(((encoded_index - 1) as usize, (handle >> 32) as u32))
}

fn next_generation(generation: u32) -> u32 {
    let next = generation.wrapping_add(1);
    if next == 0 { 1 } else { next }
}

fn set_handle_error(error: &HandleError) {
    set_last_error(ALEF_INVALID_HANDLE_ERROR, &error.to_string());
}

/// Free a string previously returned by this library.
/// # Safety
/// Pointer must have been returned by this library, or be null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_free_string(ptr: *mut c_char) {
    catch_ffi_panic((), || {
        if !ptr.is_null() {
            // SAFETY: ptr was allocated by CString::into_raw; caller ensures no aliases.
            unsafe {
                drop(CString::from_raw(ptr));
            }
        }
    })
}

/// Free a byte buffer previously returned by this library via out-params.
/// `ptr`, `len`, and `cap` must match the values written by the library function,
/// or the call must pass `ptr = null` (in which case it is a no-op).
/// # Safety
/// Pointer must have been returned by this library (via out_ptr / out_len / out_cap
/// out-params), and ownership must not already have been released.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_free_bytes(ptr: *mut u8, len: usize, cap: usize) {
    catch_ffi_panic((), || {
        if ptr.is_null() {
            return;
        }
        if len != cap {
            set_last_error(1, "invalid byte buffer metadata: len and cap differ");
            return;
        }
        // SAFETY: The library returns a Box<[u8]> allocation and reports its length
        // as both len and cap; the caller contract preserves pointer and ownership.
        unsafe {
            let slice = std::ptr::slice_from_raw_parts_mut(ptr, len);
            drop(Box::<[u8]>::from_raw(slice));
        }
    })
}

/// Return the library version string. The pointer is static and must NOT be freed.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_version() -> *const c_char {
    catch_ffi_panic(std::ptr::null(), || {
        static VERSION: &str = concat!(env!("CARGO_PKG_VERSION"), "\0");
        VERSION.as_ptr() as *const c_char
    })
}

/// Callback invoked for each streamed chunk.
/// `chunk_json` is a JSON-encoded chunk; `user_data` is forwarded from the caller.
pub type LiterllmStreamCallback =
    unsafe extern "C" fn(chunk_json: *const std::ffi::c_char, user_data: *mut std::ffi::c_void);

/// Opaque handle owning a tokio runtime and a boxed chat-stream for iterator-style consumption.
///
/// Created by `literllm_default_client_chat_stream_start`, advanced by `literllm_default_client_chat_stream_next`, destroyed by `literllm_default_client_chat_stream_free`.
/// The handle is NOT thread-safe — callers must ensure only one thread calls `_next` at a time.
pub struct LiterllmDefaultClientChatStreamStreamHandle {
    rt: tokio::runtime::Runtime,
    stream: std::sync::Mutex<
        Option<
            futures_util::stream::BoxStream<
                'static,
                Result<liter_llm::ChatCompletionChunk, Box<dyn std::error::Error + Send + Sync + 'static>>,
            >,
        >,
    >,
}

/// Start a streaming chat completion and return an opaque iterator handle.
///
/// Returns null and sets `literllm_last_error_code` on failure (null pointers or stream-open error).
/// On success the caller owns the returned pointer and MUST call `literllm_default_client_chat_stream_free` when done.
///
/// # Safety
/// `client` must be a non-null valid pointer to a live `liter_llm::DefaultClient` produced by this library.
/// `req` must be a non-null valid pointer to a live `liter_llm::ChatCompletionRequest` produced by this library.
/// Both pointers must remain valid until this function returns.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_chat_stream_start(client: AlefHandle, req: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();

        let requests = [
            HandleRequest {
                handle: client,
                expected_type: std::any::TypeId::of::<liter_llm::DefaultClient>(),
            },
            HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::ChatCompletionRequest>(),
            },
        ];
        let values = match acquire_handles(&requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut guards = Vec::with_capacity(values.len());
        for (token, value) in &values {
            match value.lock() {
                Ok(guard) => guards.push((*token, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        let client_ptr = match locked_handle_ptr::<liter_llm::DefaultClient>(&mut guards, client) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let request_ptr = match locked_handle_ptr::<liter_llm::ChatCompletionRequest>(&mut guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: both registry entry guards remain held for the duration of this call.
        let client_ref = unsafe { &*client_ptr };
        // SAFETY: both registry entry guards remain held for the duration of this call.
        let req_owned = unsafe { &*request_ptr }.clone();

        // 16 MiB: tokio's ~2 MB default worker stack can overflow on a deep extraction
        // future (a nested archive member, a multi-stage OCR pipeline), and a stack overflow
        // aborts the process with SIGBUS instead of raising a catchable panic.
        const STREAM_HANDLE_RUNTIME_STACK_SIZE_BYTES: usize = 16 * 1024 * 1024;
        let rt = match tokio::runtime::Builder::new_multi_thread()
            .enable_all()
            .thread_stack_size(STREAM_HANDLE_RUNTIME_STACK_SIZE_BYTES)
            .build()
        {
            Ok(r) => r,
            Err(e) => {
                set_last_error(
                    99,
                    &format!("literllm_default_client_chat_stream_start: failed to create tokio runtime: {e}"),
                );
                return 0;
            }
        };

        let stream_result = rt.block_on(async { client_ref.chat_stream(req_owned).await });

        let raw_stream = match stream_result {
            Ok(s) => s,
            Err(e) => {
                set_last_error(
                    99,
                    &format!("literllm_default_client_chat_stream_start: failed to open stream: {e}"),
                );
                return 0;
            }
        };

        // Map the stream's concrete error type to Box<dyn Error> to erase it from the handle type.
        let mapped: futures_util::stream::BoxStream<
            'static,
            Result<liter_llm::ChatCompletionChunk, Box<dyn std::error::Error + Send + Sync + 'static>>,
        > = {
            use futures_util::StreamExt;
            Box::pin(
                raw_stream.map(|r| r.map_err(|e| Box::new(e) as Box<dyn std::error::Error + Send + Sync + 'static>)),
            )
        };

        let handle = LiterllmDefaultClientChatStreamStreamHandle {
            rt,
            stream: std::sync::Mutex::new(Some(mapped)),
        };
        match insert_handle(handle) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Advance the stream and return a heap-allocated chunk, or null.
///
/// Returns null in two cases:
/// - Clean end-of-stream: `literllm_last_error_code()` returns 0.
/// - Stream error: `literllm_last_error_code()` returns non-zero.
///
/// The returned pointer is heap-allocated and the caller MUST free it by calling
/// `literllm_default_client_ChatCompletionChunk_free` (or the appropriate type-free function).
///
/// # Safety
/// `handle` must be a non-null valid pointer previously returned by `literllm_default_client_chat_stream_start` and not yet
/// freed. Calling `_next` after `_free` is undefined behaviour. The handle must not be shared
/// across threads without external synchronisation.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_chat_stream_next(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();

        let value = match acquire_handles(&[HandleRequest {
            handle,
            expected_type: std::any::TypeId::of::<LiterllmDefaultClientChatStreamStreamHandle>(),
        }]) {
            Ok(mut values) => match values.pop() {
                Some(value) => value,
                None => {
                    set_handle_error(&HandleError::UnknownSlot);
                    return 0;
                }
            },
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let h_guard = match value.1.lock() {
            Ok(guard) => guard,
            Err(_) => {
                set_handle_error(&HandleError::RegistryPoisoned);
                return 0;
            }
        };
        let h = match h_guard.downcast_ref::<LiterllmDefaultClientChatStreamStreamHandle>() {
            Some(value) => value,
            None => {
                set_handle_error(&HandleError::WrongType);
                return 0;
            }
        };

        let mut guard = match h.stream.lock() {
            Ok(g) => g,
            Err(_) => {
                set_last_error(99, "literllm_default_client_chat_stream_next: stream mutex is poisoned");
                return 0;
            }
        };

        let stream = match guard.as_mut() {
            Some(s) => s,
            None => {
                // Stream already exhausted or taken.
                return 0;
            }
        };

        use futures_util::StreamExt;
        match h.rt.block_on(stream.next()) {
            Some(Ok(chunk)) => {
                // SAFETY: We box the chunk and transfer ownership to the caller via raw pointer.
                // The caller must free it via the appropriate type-free function.
                match insert_handle(chunk) {
                    Ok(handle) => handle,
                    Err(error) => {
                        set_handle_error(&error);
                        0
                    }
                }
            }
            Some(Err(e)) => {
                set_last_error(
                    99,
                    &format!("literllm_default_client_chat_stream_next: stream error: {e}"),
                );
                0
            }
            None => {
                // Clean end-of-stream — error code remains 0 (cleared at top of function).
                *guard = None;
                0
            }
        }
    })
}

/// Free a stream handle created by `literllm_default_client_chat_stream_start`.
///
/// Safe to call with a null pointer (no-op). After this call the handle pointer is invalid.
///
/// # Safety
/// `handle` must either be null or a valid pointer previously returned by `literllm_default_client_chat_stream_start` and
/// not yet freed. Double-free is undefined behaviour.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_chat_stream_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<LiterllmDefaultClientChatStreamStreamHandle>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[allow(dead_code)]
fn auth_type_from_i32_rs(v: i32) -> Option<liter_llm::provider::AuthType> {
    match v {
        0 => Some(liter_llm::provider::AuthType::Bearer),
        1 => Some(liter_llm::provider::AuthType::ApiKey),
        2 => Some(liter_llm::provider::AuthType::None),
        3 => Some(liter_llm::provider::AuthType::Unknown),
        _ => None,
    }
}

#[allow(dead_code)]
fn batch_status_from_i32_rs(v: i32) -> Option<liter_llm::types::BatchStatus> {
    match v {
        0 => Some(liter_llm::types::BatchStatus::Validating),
        1 => Some(liter_llm::types::BatchStatus::Failed),
        2 => Some(liter_llm::types::BatchStatus::InProgress),
        3 => Some(liter_llm::types::BatchStatus::Finalizing),
        4 => Some(liter_llm::types::BatchStatus::Completed),
        5 => Some(liter_llm::types::BatchStatus::Expired),
        6 => Some(liter_llm::types::BatchStatus::Cancelling),
        7 => Some(liter_llm::types::BatchStatus::Cancelled),
        _ => None,
    }
}

#[cfg(feature = "tower")]
#[allow(dead_code)]
fn circuit_state_from_i32_rs(v: i32) -> Option<liter_llm::tower::CircuitState> {
    match v {
        0 => Some(liter_llm::tower::CircuitState::Closed),
        1 => Some(liter_llm::tower::CircuitState::Open),
        2 => Some(liter_llm::tower::CircuitState::HalfOpen),
        _ => None,
    }
}

#[allow(dead_code)]
fn embedding_format_from_i32_rs(v: i32) -> Option<liter_llm::types::EmbeddingFormat> {
    match v {
        0 => Some(liter_llm::types::EmbeddingFormat::Float),
        1 => Some(liter_llm::types::EmbeddingFormat::Base64),
        _ => None,
    }
}

#[cfg(feature = "tower")]
#[allow(dead_code)]
fn enforcement_from_i32_rs(v: i32) -> Option<liter_llm::Enforcement> {
    match v {
        0 => Some(liter_llm::Enforcement::Hard),
        1 => Some(liter_llm::Enforcement::Soft),
        _ => None,
    }
}

#[allow(dead_code)]
fn file_purpose_from_i32_rs(v: i32) -> Option<liter_llm::types::FilePurpose> {
    match v {
        0 => Some(liter_llm::types::FilePurpose::Assistants),
        1 => Some(liter_llm::types::FilePurpose::Batch),
        2 => Some(liter_llm::types::FilePurpose::FineTune),
        3 => Some(liter_llm::types::FilePurpose::Vision),
        _ => None,
    }
}

#[allow(dead_code)]
fn finish_reason_from_i32_rs(v: i32) -> Option<liter_llm::types::FinishReason> {
    match v {
        0 => Some(liter_llm::types::FinishReason::Stop),
        1 => Some(liter_llm::types::FinishReason::Length),
        2 => Some(liter_llm::types::FinishReason::ToolCalls),
        3 => Some(liter_llm::types::FinishReason::ContentFilter),
        4 => Some(liter_llm::types::FinishReason::FunctionCall),
        5 => Some(liter_llm::types::FinishReason::Other),
        _ => None,
    }
}

#[cfg(feature = "tower")]
#[allow(dead_code)]
fn health_status_from_i32_rs(v: i32) -> Option<liter_llm::tower::HealthStatus> {
    match v {
        0 => Some(liter_llm::tower::HealthStatus::Healthy),
        1 => Some(liter_llm::tower::HealthStatus::Unhealthy),
        _ => None,
    }
}

#[allow(dead_code)]
fn image_detail_from_i32_rs(v: i32) -> Option<liter_llm::types::ImageDetail> {
    match v {
        0 => Some(liter_llm::types::ImageDetail::Low),
        1 => Some(liter_llm::types::ImageDetail::High),
        2 => Some(liter_llm::types::ImageDetail::Auto),
        _ => None,
    }
}

#[allow(dead_code)]
fn modality_from_i32_rs(v: i32) -> Option<liter_llm::types::Modality> {
    match v {
        0 => Some(liter_llm::types::Modality::Text),
        1 => Some(liter_llm::types::Modality::Audio),
        2 => Some(liter_llm::types::Modality::Image),
        _ => None,
    }
}

#[allow(dead_code)]
fn reasoning_effort_from_i32_rs(v: i32) -> Option<liter_llm::types::ReasoningEffort> {
    match v {
        0 => Some(liter_llm::types::ReasoningEffort::Low),
        1 => Some(liter_llm::types::ReasoningEffort::Medium),
        2 => Some(liter_llm::types::ReasoningEffort::High),
        3 => Some(liter_llm::types::ReasoningEffort::Minimal),
        4 => Some(liter_llm::types::ReasoningEffort::Max),
        _ => None,
    }
}

#[allow(dead_code)]
fn refresh_outcome_from_i32_rs(v: i32) -> Option<liter_llm::cost::refresh::RefreshOutcome> {
    match v {
        0 => Some(liter_llm::cost::refresh::RefreshOutcome::Disabled),
        1 => Some(liter_llm::cost::refresh::RefreshOutcome::FromCache),
        2 => Some(liter_llm::cost::refresh::RefreshOutcome::Fetched),
        _ => None,
    }
}

#[allow(dead_code)]
fn stream_format_from_i32_rs(v: i32) -> Option<liter_llm::provider::StreamFormat> {
    match v {
        0 => Some(liter_llm::provider::StreamFormat::Sse),
        1 => Some(liter_llm::provider::StreamFormat::AwsEventStream),
        _ => None,
    }
}

#[allow(dead_code)]
fn tool_choice_mode_from_i32_rs(v: i32) -> Option<liter_llm::types::ToolChoiceMode> {
    match v {
        0 => Some(liter_llm::types::ToolChoiceMode::Auto),
        1 => Some(liter_llm::types::ToolChoiceMode::Required),
        2 => Some(liter_llm::types::ToolChoiceMode::None),
        _ => None,
    }
}

#[allow(dead_code)]
fn tool_type_from_i32_rs(v: i32) -> Option<liter_llm::types::ToolType> {
    match v {
        0 => Some(liter_llm::types::ToolType::Function),
        _ => None,
    }
}

/// Create a `AssistantMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_assistant_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::AssistantMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AssistantMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::AssistantMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AssistantMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::AssistantMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `AssistantMessage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_assistant_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.content {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `name` field from a `AssistantMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tool_calls` field from a `AssistantMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_tool_calls(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.tool_calls {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `refusal` field from a `AssistantMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_refusal(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.refusal {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `function_call` field from a `AssistantMessage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_function_call_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_function_call(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.function_call {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `reasoning_content` field from a `AssistantMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_reasoning_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::AssistantMessage, _>(handle, |obj| match &obj.reasoning_content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Return the assistant's textual response, concatenating all `Text` parts
/// if the content is structured.
///
/// Returns `None` for `Refusal`-only or `OutputImage`-only responses.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_text(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::types::AssistantMessage>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::types::AssistantMessage>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.text();
        match result {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the refusal message, if the model declined to respond.
///
/// Checks both the top-level `refusal` field and any `Refusal` parts
/// inside a structured `content`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_refusal_text(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::types::AssistantMessage>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::types::AssistantMessage>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.refusal_text();
        let result = result.map(str::to_owned);
        match result {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the model's reasoning/thinking tokens, if the provider returned any.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_reasoning_text(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::types::AssistantMessage>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::types::AssistantMessage>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.reasoning_text();
        let result = result.map(str::to_owned);
        match result {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return all `AssistantPart.OutputImage` parts in the response.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_output_images(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::types::AssistantMessage>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::types::AssistantMessage>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.output_images();
        match serde_json::to_string(&result) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return all `AssistantPart.OutputAudio` parts in the response.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_message_output_audio(this: AlefHandle) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::types::AssistantMessage>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return std::ptr::null_mut();
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::types::AssistantMessage>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return std::ptr::null_mut();
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = obj.output_audio();
        match serde_json::to_string(&result) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Create a `AudioContent` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_audio_content_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_audio_content_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::AudioContent>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AudioContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_audio_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::AudioContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AudioContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_audio_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::AudioContent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `data` field from a `AudioContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_audio_content_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::AudioContent, _>(handle, |obj| match CString::new(obj.data.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `format` field from a `AudioContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_audio_content_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::AudioContent, _>(handle, |obj| {
            match CString::new(obj.format.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `AuthConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_auth_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::provider::AuthConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `AuthConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::AuthConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AuthConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::AuthConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `auth_type` field from a `AuthConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_auth_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_config_auth_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::AuthConfig, _>(handle, |obj| {
            match insert_handle(obj.auth_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `env_var` field from a `AuthConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_config_env_var(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::AuthConfig, _>(handle, |obj| match &obj.env_var {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BatchListQuery` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_batch_list_query_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::BatchListQuery>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BatchListQuery` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::BatchListQuery, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BatchListQuery` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::BatchListQuery>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `limit` field from a `BatchListQuery`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_limit(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchListQuery, _>(handle, |obj| match &obj.limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `limit` field on a `BatchListQuery` is `Some`.
///
/// `literllm_batch_list_query_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_has_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::BatchListQuery, _>(handle, |obj| obj.limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `after` field from a `BatchListQuery`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_query_after(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchListQuery, _>(handle, |obj| match &obj.after {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BatchListResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_batch_list_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::BatchListResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BatchListResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::BatchListResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BatchListResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::BatchListResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `object` field from a `BatchListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `BatchListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `has_more` field from a `BatchListResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_has_more(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| match &obj.has_more {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `has_more` field on a `BatchListResponse` is `Some`.
///
/// `literllm_batch_list_response_has_more` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_has_has_more(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| obj.has_more.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `first_id` field from a `BatchListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_first_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| match &obj.first_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `last_id` field from a `BatchListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_list_response_last_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchListResponse, _>(handle, |obj| match &obj.last_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BatchObject` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_batch_object_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::BatchObject>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BatchObject` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::BatchObject, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BatchObject` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::BatchObject>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `endpoint` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_endpoint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| {
            match CString::new(obj.endpoint.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `input_file_id` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_input_file_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| {
            match CString::new(obj.input_file_id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `completion_window` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_completion_window(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| {
            match CString::new(obj.completion_window.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status` field from a `BatchObject`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_batch_status_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_status(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match insert_handle(obj.status.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_file_id` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_output_file_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.output_file_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `error_file_id` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_error_file_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.error_file_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created_at` field from a `BatchObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_created_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| obj.created_at);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `completed_at` field from a `BatchObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_completed_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.completed_at {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `completed_at` field on a `BatchObject` is `Some`.
///
/// `literllm_batch_object_completed_at` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_has_completed_at(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| obj.completed_at.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `failed_at` field from a `BatchObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_failed_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.failed_at {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `failed_at` field on a `BatchObject` is `Some`.
///
/// `literllm_batch_object_failed_at` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_has_failed_at(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| obj.failed_at.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `expired_at` field from a `BatchObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_expired_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.expired_at {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `expired_at` field on a `BatchObject` is `Some`.
///
/// `literllm_batch_object_expired_at` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_has_expired_at(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| obj.expired_at.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `request_counts` field from a `BatchObject`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_batch_request_counts_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_request_counts(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.request_counts {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `metadata` field from a `BatchObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_object_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::BatchObject, _>(handle, |obj| match &obj.metadata {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `BatchRequestCounts` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_batch_request_counts_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::BatchRequestCounts>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BatchRequestCounts` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::BatchRequestCounts, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BatchRequestCounts` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::BatchRequestCounts>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `total` field from a `BatchRequestCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_total(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchRequestCounts, _>(handle, |obj| obj.total);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `completed` field from a `BatchRequestCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_completed(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchRequestCounts, _>(handle, |obj| obj.completed);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `failed` field from a `BatchRequestCounts`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_request_counts_failed(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::BatchRequestCounts, _>(handle, |obj| obj.failed);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `BedrockConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_bedrock_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::BedrockConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `BedrockConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::BedrockConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BedrockConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::BedrockConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `region` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_region(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::BedrockConfig, _>(handle, |obj| match &obj.region {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cross_region_prefix` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_cross_region_prefix(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::BedrockConfig, _>(handle, |obj| match &obj.cross_region_prefix {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `access_key_id` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_access_key_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::BedrockConfig, _>(handle, |obj| match &obj.access_key_id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `secret_access_key` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_secret_access_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::BedrockConfig, _>(handle, |obj| match &obj.secret_access_key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `session_token` field from a `BedrockConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_bedrock_config_session_token(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::BedrockConfig, _>(handle, |obj| match &obj.session_token {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Create a `BudgetConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_budget_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::BudgetConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `BudgetConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::BudgetConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `BudgetConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::BudgetConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `global_limit` field from a `BudgetConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_global_limit(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::BudgetConfig, _>(handle, |obj| match &obj.global_limit {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Report whether the `global_limit` field on a `BudgetConfig` is `Some`.
///
/// `literllm_budget_config_global_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_has_global_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::BudgetConfig, _>(handle, |obj| obj.global_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `model_limits` field from a `BudgetConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_model_limits(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::BudgetConfig, _>(handle, |obj| match serde_json::to_string(&obj.model_limits) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `enforcement` field from a `BudgetConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_enforcement_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_enforcement(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::BudgetConfig, _>(handle, |obj| match insert_handle(obj.enforcement) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tower")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_budget_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = liter_llm::BudgetConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

#[cfg(feature = "tower")]
/// Create a `CacheConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_cache_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::CacheConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `CacheConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::CacheConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `CacheConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::CacheConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `max_entries` field from a `CacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_max_entries(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::CacheConfig, _>(handle, |obj| obj.max_entries);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `ttl` field from a `CacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_ttl(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::CacheConfig, _>(handle, |obj| obj.ttl.as_millis() as u64);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `backend` field from a `CacheConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_cache_backend_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_backend(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::CacheConfig, _>(handle, |obj| match insert_handle(obj.backend.clone()) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tower")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = liter_llm::CacheConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `CatalogRefreshConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_catalog_refresh_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::cost::refresh::CatalogRefreshConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CatalogRefreshConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::cost::refresh::CatalogRefreshConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CatalogRefreshConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::cost::refresh::CatalogRefreshConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `enabled` field from a `CatalogRefreshConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_enabled(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::cost::refresh::CatalogRefreshConfig, _>(handle, |obj| obj.enabled as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `source_url` field from a `CatalogRefreshConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_source_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::cost::refresh::CatalogRefreshConfig, _>(handle, |obj| {
                match CString::new(obj.source_url.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `ttl_seconds` field from a `CatalogRefreshConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_ttl_seconds(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::cost::refresh::CatalogRefreshConfig, _>(handle, |obj| obj.ttl_seconds);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cache_path` field from a `CatalogRefreshConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_cache_path(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::cost::refresh::CatalogRefreshConfig, _>(handle, |obj| match &obj.cache_path {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_catalog_refresh_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = liter_llm::cost::refresh::CatalogRefreshConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `ChatCompletionChunk` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_chat_completion_chunk_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ChatCompletionChunk>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChatCompletionChunk` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChatCompletionChunk` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ChatCompletionChunk>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| {
            match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created` field from a `ChatCompletionChunk`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_created(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| obj.created);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `model` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `choices` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_choices(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| {
                match serde_json::to_string(&obj.choices) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `usage` field from a `ChatCompletionChunk`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_usage_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_usage(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| match &obj.usage {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `system_fingerprint` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_system_fingerprint(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| match &obj.system_fingerprint {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `service_tier` field from a `ChatCompletionChunk`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_chunk_service_tier(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionChunk, _>(handle, |obj| match &obj.service_tier {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChatCompletionRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_chat_completion_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ChatCompletionRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChatCompletionRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChatCompletionRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ChatCompletionRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `messages` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_messages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
                match serde_json::to_string(&obj.messages) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `temperature` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_temperature(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.temperature {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `temperature` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_temperature` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_temperature(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.temperature.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `top_p` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_top_p(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.top_p {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `top_p` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_top_p` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_top_p(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.top_p.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `n` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_n(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.n {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `n` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_n` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_n(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.n.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `stream` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_stream(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.stream {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `stream` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_stream` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_stream(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.stream.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `stop` field from a `ChatCompletionRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_stop_sequence_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_stop(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.stop {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `max_tokens` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_max_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.max_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_tokens` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_max_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_max_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.max_tokens.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `presence_penalty` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_presence_penalty(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.presence_penalty {
                Some(val) => *val,
                None => 0.0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `presence_penalty` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_presence_penalty` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_presence_penalty(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
            obj.presence_penalty.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `frequency_penalty` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_frequency_penalty(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.frequency_penalty {
                Some(val) => *val,
                None => 0.0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `frequency_penalty` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_frequency_penalty` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_frequency_penalty(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
            obj.frequency_penalty.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `logit_bias` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_logit_bias(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.logit_bias {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `user` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_user(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.user {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tools` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_tools(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.tools {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tool_choice` field from a `ChatCompletionRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_tool_choice_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_tool_choice(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.tool_choice {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `parallel_tool_calls` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_parallel_tool_calls(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.parallel_tool_calls {
                Some(val) => *val as i32,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `parallel_tool_calls` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_parallel_tool_calls` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_parallel_tool_calls(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
            obj.parallel_tool_calls.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `response_format` field from a `ChatCompletionRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_response_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_response_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.response_format {
                Some(val) => match insert_handle(val.clone()) {
                    Ok(handle) => handle,
                    Err(error) => {
                        set_handle_error(&error);
                        0
                    }
                },
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `stream_options` field from a `ChatCompletionRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_stream_options_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_stream_options(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.stream_options {
                Some(val) => match insert_handle(val.clone()) {
                    Ok(handle) => handle,
                    Err(error) => {
                        set_handle_error(&error);
                        0
                    }
                },
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `seed` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_seed(handle: AlefHandle) -> i64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.seed {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `seed` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_seed` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_seed(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.seed.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `reasoning_effort` field from a `ChatCompletionRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_reasoning_effort_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_reasoning_effort(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.reasoning_effort {
                Some(val) => match insert_handle(val.clone()) {
                    Ok(handle) => handle,
                    Err(error) => {
                        set_handle_error(&error);
                        0
                    }
                },
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `modalities` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_modalities(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.modalities {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `logprobs` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_logprobs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.logprobs {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `logprobs` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_logprobs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_logprobs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.logprobs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `top_logprobs` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_top_logprobs(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.top_logprobs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `top_logprobs` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_top_logprobs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_top_logprobs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.top_logprobs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_completion_tokens` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_max_completion_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.max_completion_tokens {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_completion_tokens` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_max_completion_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_max_completion_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| {
            obj.max_completion_tokens.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `service_tier` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_service_tier(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.service_tier {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `store` field from a `ChatCompletionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_store(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.store {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `store` field on a `ChatCompletionRequest` is `Some`.
///
/// `literllm_chat_completion_request_store` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_has_store(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| obj.store.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `metadata` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.metadata {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `prediction` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_prediction(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.prediction {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `audio` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_audio(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.audio {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `web_search_options` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_web_search_options(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.web_search_options {
                Some(val) => match serde_json::to_string(&val) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `extra_body` field from a `ChatCompletionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_request_extra_body(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionRequest, _>(handle, |obj| match &obj.extra_body {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChatCompletionResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_chat_completion_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ChatCompletionResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChatCompletionResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChatCompletionResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ChatCompletionResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| {
            match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created` field from a `ChatCompletionResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_created(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| obj.created);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `model` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `choices` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_choices(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.choices) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `usage` field from a `ChatCompletionResponse`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_usage_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_usage(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| match &obj.usage {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `system_fingerprint` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_system_fingerprint(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| match &obj.system_fingerprint {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `service_tier` field from a `ChatCompletionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_response_service_tier(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ChatCompletionResponse, _>(handle, |obj| match &obj.service_tier {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ChatCompletionTool` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_chat_completion_tool_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_tool_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ChatCompletionTool>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ChatCompletionTool` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_tool_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ChatCompletionTool, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ChatCompletionTool` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_tool_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ChatCompletionTool>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `tool_type` field from a `ChatCompletionTool`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_tool_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_tool_tool_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionTool, _>(handle, |obj| {
            match insert_handle(obj.tool_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `function` field from a `ChatCompletionTool`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_function_definition_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_chat_completion_tool_function(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ChatCompletionTool, _>(handle, |obj| {
            match insert_handle(obj.function.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `Choice` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_choice_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::Choice>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Choice` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::Choice, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Choice` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::Choice>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `index` field from a `Choice`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Choice, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `message` field from a `Choice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_assistant_message_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_message(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::Choice, _>(handle, |obj| match insert_handle(obj.message.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `finish_reason` field from a `Choice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_finish_reason_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_finish_reason(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Choice, _>(handle, |obj| match &obj.finish_reason {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `logprobs` field from a `Choice`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_choice_logprobs(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::Choice, _>(handle, |obj| match &obj.logprobs {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CreateBatchRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_batch_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateBatchRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateBatchRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateBatchRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateBatchRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateBatchRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `input_file_id` field from a `CreateBatchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_input_file_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateBatchRequest, _>(handle, |obj| {
            match CString::new(obj.input_file_id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `endpoint` field from a `CreateBatchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_endpoint(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateBatchRequest, _>(handle, |obj| {
            match CString::new(obj.endpoint.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `completion_window` field from a `CreateBatchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_completion_window(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateBatchRequest, _>(handle, |obj| {
            match CString::new(obj.completion_window.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `metadata` field from a `CreateBatchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_batch_request_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateBatchRequest, _>(handle, |obj| match &obj.metadata {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CreateFileRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_file_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateFileRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateFileRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateFileRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateFileRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateFileRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `file` field from a `CreateFileRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateFileRequest, _>(handle, |obj| {
            match CString::new(obj.file.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `purpose` field from a `CreateFileRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_file_purpose_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_purpose(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::CreateFileRequest, _>(handle, |obj| {
            match insert_handle(obj.purpose.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `filename` field from a `CreateFileRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_file_request_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateFileRequest, _>(handle, |obj| match &obj.filename {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CreateImageRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_image_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateImageRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateImageRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateImageRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateImageRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateImageRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `prompt` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| {
            match CString::new(obj.prompt.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.model {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `n` field from a `CreateImageRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_n(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.n {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `n` field on a `CreateImageRequest` is `Some`.
///
/// `literllm_create_image_request_n` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_has_n(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| obj.n.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `size` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_size(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.size {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `quality` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_quality(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.quality {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `style` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_style(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.style {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `response_format` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_response_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.response_format {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `user` field from a `CreateImageRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_image_request_user(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateImageRequest, _>(handle, |obj| match &obj.user {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `CreateResponseRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_response_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateResponseRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateResponseRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateResponseRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateResponseRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `input` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_input(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| {
                match serde_json::to_string(&obj.input) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `instructions` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_instructions(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.instructions {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tools` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_tools(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.tools {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `temperature` field from a `CreateResponseRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_temperature(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.temperature {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `temperature` field on a `CreateResponseRequest` is `Some`.
///
/// `literllm_create_response_request_temperature` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_has_temperature(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| obj.temperature.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_output_tokens` field from a `CreateResponseRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_max_output_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.max_output_tokens {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_output_tokens` field on a `CreateResponseRequest` is `Some`.
///
/// `literllm_create_response_request_max_output_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_has_max_output_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| {
            obj.max_output_tokens.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `metadata` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_metadata(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.metadata {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `extra_body` field from a `CreateResponseRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_extra_body(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.extra_body {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `stream` field from a `CreateResponseRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_stream(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| match &obj.stream {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `stream` field on a `CreateResponseRequest` is `Some`.
///
/// `literllm_create_response_request_stream` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_response_request_has_stream(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::CreateResponseRequest, _>(handle, |obj| obj.stream.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `CreateSpeechRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_speech_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateSpeechRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateSpeechRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateSpeechRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateSpeechRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `CreateSpeechRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `input` field from a `CreateSpeechRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_input(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| {
            match CString::new(obj.input.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `voice` field from a `CreateSpeechRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_voice(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| {
            match CString::new(obj.voice.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `response_format` field from a `CreateSpeechRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_response_format(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| match &obj.response_format {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `speed` field from a `CreateSpeechRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_speed(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| match &obj.speed {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `speed` field on a `CreateSpeechRequest` is `Some`.
///
/// `literllm_create_speech_request_speed` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_speech_request_has_speed(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::CreateSpeechRequest, _>(handle, |obj| obj.speed.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `CreateTranscriptionRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_create_transcription_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::CreateTranscriptionRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CreateTranscriptionRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CreateTranscriptionRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::CreateTranscriptionRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `CreateTranscriptionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `file` field from a `CreateTranscriptionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_file(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| {
            match CString::new(obj.file.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `CreateTranscriptionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| match &obj.language {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `prompt` field from a `CreateTranscriptionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| match &obj.prompt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `response_format` field from a `CreateTranscriptionRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_response_format(
    handle: AlefHandle,
) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| match &obj.response_format {
                Some(val) => match CString::new(val.to_string()) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI field value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                None => std::ptr::null_mut(),
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `temperature` field from a `CreateTranscriptionRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_temperature(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result =
            with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| match &obj.temperature {
                Some(val) => *val,
                None => 0.0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `temperature` field on a `CreateTranscriptionRequest` is `Some`.
///
/// `literllm_create_transcription_request_temperature` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_transcription_request_has_temperature(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::CreateTranscriptionRequest, _>(handle, |obj| {
            obj.temperature.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `CustomProviderConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_custom_provider_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::provider::custom::CustomProviderConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `CustomProviderConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::custom::CustomProviderConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `CustomProviderConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::custom::CustomProviderConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `CustomProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::custom::CustomProviderConfig, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `CustomProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::custom::CustomProviderConfig, _>(handle, |obj| {
            match CString::new(obj.base_url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `auth_header` field from a `CustomProviderConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_auth_header_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_auth_header(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::custom::CustomProviderConfig, _>(handle, |obj| {
            match insert_handle(obj.auth_header.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `model_prefixes` field from a `CustomProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_custom_provider_config_model_prefixes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::custom::CustomProviderConfig, _>(handle, |obj| {
            match serde_json::to_string(&obj.model_prefixes) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DecodedDataUrl` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_decoded_data_url_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decoded_data_url_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::image::DecodedDataUrl>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DecodedDataUrl` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decoded_data_url_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::image::DecodedDataUrl, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DecodedDataUrl` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decoded_data_url_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::image::DecodedDataUrl>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `mime` field from a `DecodedDataUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decoded_data_url_mime(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::image::DecodedDataUrl, _>(handle, |obj| {
            match CString::new(obj.mime.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `DecodedDataUrl`.
/// The returned byte pointer is borrowed from `ptr` and must not be freed.
/// It remains valid until `ptr` is destroyed or the field is mutated.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decoded_data_url_data(handle: AlefHandle, out_len: *mut usize) -> *mut u8 {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::image::DecodedDataUrl, _>(handle, |obj| {
            let data = &obj.data;
            if !out_len.is_null() {
                // SAFETY: null check above guarantees out_len is a valid pointer.
                unsafe {
                    *out_len = data.len();
                }
            }
            data.as_ptr() as *mut u8
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Free a `DefaultClient` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::DefaultClient>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_chat(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::ChatCompletionRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::ChatCompletionRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.chat(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_chat_stream(
    client: AlefHandle,
    request_json: *const std::ffi::c_char,
    callback: LiterllmStreamCallback,
    user_data: *mut std::ffi::c_void,
) -> i32 {
    catch_ffi_panic(1, || {
        clear_last_error();
        let requests = [HandleRequest {
            handle: client,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        }];
        let values = match acquire_handles(&requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut guards = Vec::with_capacity(values.len());
        for (token, value) in &values {
            match value.lock() {
                Ok(guard) => guards.push((*token, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        let client: *mut liter_llm::client::DefaultClient =
            match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut guards, client) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return -1;
                }
            };
        clear_last_error();

        if client.is_null() {
            set_last_error(99, "literllm_chat_stream: client must not be NULL");
            return -1;
        }
        if request_json.is_null() {
            set_last_error(99, "literllm_chat_stream: request_json must not be NULL");
            return -1;
        }

        // SAFETY: caller guarantees `client` and `request_json` are non-null and valid.
        let client_ref = unsafe { &(*client) };

        let json_str = match unsafe { std::ffi::CStr::from_ptr(request_json) }.to_str() {
            Ok(s) => s,
            Err(e) => {
                set_last_error(
                    99,
                    &format!("literllm_chat_stream: request_json is not valid UTF-8: {e}"),
                );
                return -1;
            }
        };

        let request: liter_llm::ChatCompletionRequest = match serde_json::from_str(json_str) {
            Ok(r) => r,
            Err(e) => {
                set_last_error(99, &format!("literllm_chat_stream: failed to parse request JSON: {e}"));
                return -1;
            }
        };

        let rt = get_ffi_runtime();

        let result = rt.block_on(async {
            use futures_util::StreamExt;

            let mut stream = match client_ref.chat_stream(request).await {
                Ok(s) => s,
                Err(e) => return Err(format!("literllm_chat_stream: failed to open stream: {e}")),
            };

            loop {
                match stream.next().await {
                    None => break,
                    Some(Err(e)) => return Err(format!("literllm_chat_stream: stream error: {e}")),
                    Some(Ok(chunk)) => {
                        let chunk_json = match serde_json::to_string(&chunk) {
                            Ok(s) => s,
                            Err(e) => return Err(format!("literllm_chat_stream: failed to serialise chunk: {e}")),
                        };
                        match std::ffi::CString::new(chunk_json) {
                            Ok(c_str) => {
                                // SAFETY: `callback` is a valid function pointer supplied by the caller.
                                // `c_str.as_ptr()` is valid for this block scope.
                                // `user_data` is forwarded as-is; ownership stays with the caller.
                                unsafe { callback(c_str.as_ptr(), user_data) };
                            }
                            Err(e) => return Err(format!("literllm_chat_stream: chunk JSON contained NUL byte: {e}")),
                        }
                    }
                }
            }
            Ok(())
        });

        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(99, &e);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_embed(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::EmbeddingRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::EmbeddingRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.embed(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_list_models(this: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let result = get_ffi_runtime().block_on(async { obj.list_models().await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_image_generate(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateImageRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::CreateImageRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.image_generate(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_speech(
    this: AlefHandle,
    req: AlefHandle,
    out_ptr: *mut *mut u8,
    out_len: *mut usize,
    out_cap: *mut usize,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if out_ptr.is_null() || out_len.is_null() || out_cap.is_null() {
            set_last_error(1, "Null out-param pointer for byte buffer return");
            return -1;
        }
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateSpeechRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return -1;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::CreateSpeechRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.speech(req_rs).await });
        match result {
            Ok(val) => {
                // Copy through a `&[u8]` slice so the path works for every byte-ish return shape:
                // an owned `Vec<u8>` or `bytes::Bytes`, and equally a *borrowed* `&Bytes`/`&[u8]`
                // from an accessor like `fn as_bytes(&self) -> &Bytes`. `Vec::<u8>::from(val)`
                // handled only the owned cases -- there is no `From<&Bytes> for Vec<u8>`, so a
                // borrowing accessor failed to compile. Indexing with `[..]` derefs uniformly. The
                // slice is redundant for the owned shapes and load-bearing for the borrowed ones,
                // and this template cannot tell them apart, so the lint is allowed rather than the
                // expression narrowed.
                #[allow(clippy::redundant_slicing)]
                let buffer = Vec::<u8>::from(&val[..]).into_boxed_slice();
                let len = buffer.len();
                let ptr = Box::into_raw(buffer).cast::<u8>();
                // SAFETY: out_ptr/out_len/out_cap were null-checked above.
                unsafe {
                    *out_ptr = ptr;
                    *out_len = len;
                    *out_cap = len;
                }
                0
            }
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                // SAFETY: out_ptr/out_len/out_cap were null-checked above.
                unsafe {
                    *out_ptr = std::ptr::null_mut();
                    *out_len = 0;
                    *out_cap = 0;
                }
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_transcribe(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateTranscriptionRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr =
            match locked_handle_ptr::<liter_llm::types::CreateTranscriptionRequest>(&mut __alef_guards, req) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.transcribe(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_moderate(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::ModerationRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::ModerationRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.moderate(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_rerank(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::RerankRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::RerankRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.rerank(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_search(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::SearchRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::SearchRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.search(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_ocr(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::OcrRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::OcrRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.ocr(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_create_file(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateFileRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::CreateFileRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.create_file(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_retrieve_file(
    this: AlefHandle,
    file_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if file_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'file_id'");
            return 0;
        }
        // SAFETY: null check above guarantees file_id is a valid pointer; string is valid UTF-8 from caller.
        let file_id_rs = match unsafe { CStr::from_ptr(file_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'file_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.retrieve_file(&file_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_delete_file(
    this: AlefHandle,
    file_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if file_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'file_id'");
            return 0;
        }
        // SAFETY: null check above guarantees file_id is a valid pointer; string is valid UTF-8 from caller.
        let file_id_rs = match unsafe { CStr::from_ptr(file_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'file_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.delete_file(&file_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_list_files(this: AlefHandle, query: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            if query != 0 {
                Some(HandleRequest {
                    handle: query,
                    expected_type: std::any::TypeId::of::<liter_llm::types::FileListQuery>(),
                })
            } else {
                None
            },
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let query_rs = if query == 0 {
            None
        } else {
            let value = match locked_handle_ptr::<liter_llm::types::FileListQuery>(&mut __alef_guards, query) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
            // SAFETY: the registry entry guard remains held for the duration of this call.
            Some(unsafe { &*value }.clone())
        };

        let result = get_ffi_runtime().block_on(async { obj.list_files(query_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_file_content(
    this: AlefHandle,
    file_id: *const std::ffi::c_char,
    out_ptr: *mut *mut u8,
    out_len: *mut usize,
    out_cap: *mut usize,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if out_ptr.is_null() || out_len.is_null() || out_cap.is_null() {
            set_last_error(1, "Null out-param pointer for byte buffer return");
            return -1;
        }
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return -1;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if file_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'file_id'");
            return -1;
        }
        // SAFETY: null check above guarantees file_id is a valid pointer; string is valid UTF-8 from caller.
        let file_id_rs = match unsafe { CStr::from_ptr(file_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'file_id'");
                return -1;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.file_content(&file_id_rs).await });
        match result {
            Ok(val) => {
                // Copy through a `&[u8]` slice so the path works for every byte-ish return shape:
                // an owned `Vec<u8>` or `bytes::Bytes`, and equally a *borrowed* `&Bytes`/`&[u8]`
                // from an accessor like `fn as_bytes(&self) -> &Bytes`. `Vec::<u8>::from(val)`
                // handled only the owned cases -- there is no `From<&Bytes> for Vec<u8>`, so a
                // borrowing accessor failed to compile. Indexing with `[..]` derefs uniformly. The
                // slice is redundant for the owned shapes and load-bearing for the borrowed ones,
                // and this template cannot tell them apart, so the lint is allowed rather than the
                // expression narrowed.
                #[allow(clippy::redundant_slicing)]
                let buffer = Vec::<u8>::from(&val[..]).into_boxed_slice();
                let len = buffer.len();
                let ptr = Box::into_raw(buffer).cast::<u8>();
                // SAFETY: out_ptr/out_len/out_cap were null-checked above.
                unsafe {
                    *out_ptr = ptr;
                    *out_len = len;
                    *out_cap = len;
                }
                0
            }
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                // SAFETY: out_ptr/out_len/out_cap were null-checked above.
                unsafe {
                    *out_ptr = std::ptr::null_mut();
                    *out_len = 0;
                    *out_cap = 0;
                }
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_create_batch(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateBatchRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::CreateBatchRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.create_batch(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_retrieve_batch(
    this: AlefHandle,
    batch_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if batch_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'batch_id'");
            return 0;
        }
        // SAFETY: null check above guarantees batch_id is a valid pointer; string is valid UTF-8 from caller.
        let batch_id_rs = match unsafe { CStr::from_ptr(batch_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'batch_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.retrieve_batch(&batch_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_list_batches(this: AlefHandle, query: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            if query != 0 {
                Some(HandleRequest {
                    handle: query,
                    expected_type: std::any::TypeId::of::<liter_llm::types::BatchListQuery>(),
                })
            } else {
                None
            },
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        let query_rs = if query == 0 {
            None
        } else {
            let value = match locked_handle_ptr::<liter_llm::types::BatchListQuery>(&mut __alef_guards, query) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
            // SAFETY: the registry entry guard remains held for the duration of this call.
            Some(unsafe { &*value }.clone())
        };

        let result = get_ffi_runtime().block_on(async { obj.list_batches(query_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_cancel_batch(
    this: AlefHandle,
    batch_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if batch_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'batch_id'");
            return 0;
        }
        // SAFETY: null check above guarantees batch_id is a valid pointer; string is valid UTF-8 from caller.
        let batch_id_rs = match unsafe { CStr::from_ptr(batch_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'batch_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.cancel_batch(&batch_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_fetch_batch_for_polling(
    this: AlefHandle,
    batch_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if batch_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'batch_id'");
            return 0;
        }
        // SAFETY: null check above guarantees batch_id is a valid pointer; string is valid UTF-8 from caller.
        let batch_id_rs = match unsafe { CStr::from_ptr(batch_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'batch_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.fetch_batch_for_polling(&batch_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Poll a batch until it reaches a terminal status (Completed, Failed, Expired, Cancelled).
///
/// Uses exponential backoff with configurable initial interval, maximum interval, and backoff
/// multiplier.
/// Optionally supports a timeout that aborts polling if exceeded.
/// \note Returns `BatchWaitError::Failed` if the batch reaches a failure terminal status.
/// Returns `BatchWaitError::Timeout` if the configured timeout is exceeded.
/// Returns `BatchWaitError::Client` for underlying client errors.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// # use liter_llm::client::{DefaultClient, ClientConfig, WaitForBatchConfig};
/// # async fn run() -> Result<(), Box<dyn std::error::Error>> {
/// let client = DefaultClient::new(ClientConfig::new("api-key"), None)?;
/// let batch = client.wait_for_batch("b-123", WaitForBatchConfig::default()).await?;
/// println!("Batch completed: {:?}", batch.status);
/// # Ok(())
/// # }
/// \endcode
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_wait_for_batch(
    this: AlefHandle,
    batch_id: *const std::ffi::c_char,
    config: AlefHandle,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: config,
                expected_type: std::any::TypeId::of::<liter_llm::client::WaitForBatchConfig>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if batch_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'batch_id'");
            return 0;
        }
        // SAFETY: null check above guarantees batch_id is a valid pointer; string is valid UTF-8 from caller.
        let batch_id_rs = match unsafe { CStr::from_ptr(batch_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'batch_id'");
                return 0;
            }
        };

        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr = match locked_handle_ptr::<liter_llm::client::WaitForBatchConfig>(&mut __alef_guards, config)
        {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.wait_for_batch(&batch_id_rs, config_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_create_response(this: AlefHandle, req: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [
            Some(HandleRequest {
                handle: this,
                expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
            }),
            Some(HandleRequest {
                handle: req,
                expected_type: std::any::TypeId::of::<liter_llm::types::CreateResponseRequest>(),
            }),
        ]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::CreateResponseRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr }.clone();

        let result = get_ffi_runtime().block_on(async { obj.create_response(req_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_retrieve_response(
    this: AlefHandle,
    response_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if response_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'response_id'");
            return 0;
        }
        // SAFETY: null check above guarantees response_id is a valid pointer; string is valid UTF-8 from caller.
        let response_id_rs = match unsafe { CStr::from_ptr(response_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'response_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.retrieve_response(&response_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_default_client_cancel_response(
    this: AlefHandle,
    response_id: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: this,
            expected_type: std::any::TypeId::of::<liter_llm::client::DefaultClient>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if this == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return 0;
        }
        let obj_ptr = match locked_handle_ptr::<liter_llm::client::DefaultClient>(&mut __alef_guards, this) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let obj = unsafe { &*obj_ptr };

        if response_id.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'response_id'");
            return 0;
        }
        // SAFETY: null check above guarantees response_id is a valid pointer; string is valid UTF-8 from caller.
        let response_id_rs = match unsafe { CStr::from_ptr(response_id) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'response_id'");
                return 0;
            }
        };

        let result = get_ffi_runtime().block_on(async { obj.cancel_response(&response_id_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `DeleteResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_delete_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::DeleteResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DeleteResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::DeleteResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DeleteResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::DeleteResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `DeleteResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::DeleteResponse, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `DeleteResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::DeleteResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `deleted` field from a `DeleteResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_delete_response_deleted(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::DeleteResponse, _>(handle, |obj| obj.deleted as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `DeveloperMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_developer_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_developer_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::DeveloperMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DeveloperMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_developer_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::DeveloperMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DeveloperMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_developer_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::DeveloperMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `DeveloperMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_developer_message_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::DeveloperMessage, _>(handle, |obj| {
            match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `name` field from a `DeveloperMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_developer_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::DeveloperMessage, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `DocumentContent` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_document_content_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_document_content_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::DocumentContent>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `DocumentContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_document_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::DocumentContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `DocumentContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_document_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::DocumentContent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `data` field from a `DocumentContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_document_content_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::DocumentContent, _>(handle, |obj| {
            match CString::new(obj.data.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `media_type` field from a `DocumentContent`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_document_content_media_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::DocumentContent, _>(handle, |obj| {
            match CString::new(obj.media_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmbeddingObject` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_embedding_object_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::EmbeddingObject>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmbeddingObject` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::EmbeddingObject, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingObject` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::EmbeddingObject>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `object` field from a `EmbeddingObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingObject, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `embedding` field from a `EmbeddingObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_embedding(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingObject, _>(handle, |obj| {
            match serde_json::to_string(&obj.embedding) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `index` field from a `EmbeddingObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_object_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::EmbeddingObject, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `EmbeddingRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_embedding_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::EmbeddingRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmbeddingRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::EmbeddingRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `EmbeddingRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `input` field from a `EmbeddingRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_embedding_input_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_input(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| {
            match insert_handle(obj.input.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `encoding_format` field from a `EmbeddingRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_embedding_format_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_encoding_format(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| match &obj.encoding_format {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `dimensions` field from a `EmbeddingRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_dimensions(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| match &obj.dimensions {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `dimensions` field on a `EmbeddingRequest` is `Some`.
///
/// `literllm_embedding_request_dimensions` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_has_dimensions(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| obj.dimensions.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `user` field from a `EmbeddingRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_request_user(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingRequest, _>(handle, |obj| match &obj.user {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `EmbeddingResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_embedding_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::EmbeddingResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `EmbeddingResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::EmbeddingResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::EmbeddingResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `object` field from a `EmbeddingResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `EmbeddingResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `EmbeddingResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::EmbeddingResponse, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `usage` field from a `EmbeddingResponse`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_usage_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_response_usage(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::EmbeddingResponse, _>(handle, |obj| match &obj.usage {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `FileListQuery` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_file_list_query_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FileListQuery>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FileListQuery` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FileListQuery, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FileListQuery` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FileListQuery>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `purpose` field from a `FileListQuery`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_purpose(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileListQuery, _>(handle, |obj| match &obj.purpose {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `limit` field from a `FileListQuery`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_limit(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::FileListQuery, _>(handle, |obj| match &obj.limit {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `limit` field on a `FileListQuery` is `Some`.
///
/// `literllm_file_list_query_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_has_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::FileListQuery, _>(handle, |obj| obj.limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `after` field from a `FileListQuery`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_query_after(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileListQuery, _>(handle, |obj| match &obj.after {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FileListResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_file_list_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FileListResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FileListResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FileListResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FileListResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FileListResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `object` field from a `FileListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileListResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `FileListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileListResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `has_more` field from a `FileListResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_has_more(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::FileListResponse, _>(handle, |obj| match &obj.has_more {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `has_more` field on a `FileListResponse` is `Some`.
///
/// `literllm_file_list_response_has_more` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_list_response_has_has_more(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::FileListResponse, _>(handle, |obj| obj.has_more.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `FileObject` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_file_object_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FileObject>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FileObject` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FileObject, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FileObject` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FileObject>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `FileObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::FileObject, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `FileObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::FileObject, _>(handle, |obj| match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `bytes` field from a `FileObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_bytes(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::FileObject, _>(handle, |obj| obj.bytes);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `created_at` field from a `FileObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_created_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::FileObject, _>(handle, |obj| obj.created_at);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `filename` field from a `FileObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_filename(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileObject, _>(handle, |obj| {
            match CString::new(obj.filename.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `purpose` field from a `FileObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_purpose(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::FileObject, _>(handle, |obj| match CString::new(obj.purpose.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status` field from a `FileObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_object_status(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FileObject, _>(handle, |obj| match &obj.status {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FunctionCall` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_function_call_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_call_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FunctionCall>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FunctionCall` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_call_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FunctionCall, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FunctionCall` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_call_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FunctionCall>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `FunctionCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_call_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::FunctionCall, _>(handle, |obj| match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `arguments` field from a `FunctionCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_call_arguments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionCall, _>(handle, |obj| {
            match CString::new(obj.arguments.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `FunctionDefinition` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_function_definition_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FunctionDefinition>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FunctionDefinition` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FunctionDefinition, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FunctionDefinition` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FunctionDefinition>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `FunctionDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionDefinition, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `FunctionDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionDefinition, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `parameters` field from a `FunctionDefinition`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_parameters(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionDefinition, _>(handle, |obj| match &obj.parameters {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strict` field from a `FunctionDefinition`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_strict(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::FunctionDefinition, _>(handle, |obj| match &obj.strict {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `strict` field on a `FunctionDefinition` is `Some`.
///
/// `literllm_function_definition_strict` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_definition_has_strict(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::FunctionDefinition, _>(handle, |obj| obj.strict.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `FunctionMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_function_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::FunctionMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `FunctionMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FunctionMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FunctionMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FunctionMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `FunctionMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_message_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionMessage, _>(handle, |obj| {
            match CString::new(obj.content.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `name` field from a `FunctionMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_function_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::FunctionMessage, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Image` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_image_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::Image>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Image` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::Image, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Image` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::Image>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `url` field from a `Image`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::Image, _>(handle, |obj| match &obj.url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `b64_json` field from a `Image`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_b64_json(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::Image, _>(handle, |obj| match &obj.b64_json {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `revised_prompt` field from a `Image`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_revised_prompt(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::Image, _>(handle, |obj| match &obj.revised_prompt {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ImageUrl` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_image_url_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ImageUrl>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImageUrl` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ImageUrl, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImageUrl` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ImageUrl>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `url` field from a `ImageUrl`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ImageUrl, _>(handle, |obj| match CString::new(obj.url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `detail` field from a `ImageUrl`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_image_detail_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_url_detail(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ImageUrl, _>(handle, |obj| match &obj.detail {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ImagesResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_images_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_images_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ImagesResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ImagesResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_images_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ImagesResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImagesResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_images_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ImagesResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `created` field from a `ImagesResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_images_response_created(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ImagesResponse, _>(handle, |obj| obj.created);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `data` field from a `ImagesResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_images_response_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ImagesResponse, _>(handle, |obj| match serde_json::to_string(&obj.data) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Create a `InFlightLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_in_flight_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_in_flight_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::InFlightLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `InFlightLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_in_flight_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::InFlightLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `InFlightLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_in_flight_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::InFlightLimitConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `max_in_flight` field from a `InFlightLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_in_flight_limit_config_max_in_flight(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::InFlightLimitConfig, _>(handle, |obj| match &obj.max_in_flight {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Report whether the `max_in_flight` field on a `InFlightLimitConfig` is `Some`.
///
/// `literllm_in_flight_limit_config_max_in_flight` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_in_flight_limit_config_has_max_in_flight(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::InFlightLimitConfig, _>(handle, |obj| obj.max_in_flight.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `IntentPrototype` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_intent_prototype_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::tower::IntentPrototype>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `name` field from a `IntentPrototype`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_intent_prototype_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::tower::IntentPrototype, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `embedding` field from a `IntentPrototype`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_intent_prototype_embedding(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::tower::IntentPrototype, _>(handle, |obj| {
            match serde_json::to_string(&obj.embedding) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `model` field from a `IntentPrototype`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_intent_prototype_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::tower::IntentPrototype, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `JsonSchemaFormat` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_json_schema_format_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::JsonSchemaFormat>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `JsonSchemaFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `JsonSchemaFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::JsonSchemaFormat>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `JsonSchemaFormat`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `description` field from a `JsonSchemaFormat`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_description(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, |obj| match &obj.description {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `schema` field from a `JsonSchemaFormat`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_schema(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, |obj| {
            match serde_json::to_string(&obj.schema) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `strict` field from a `JsonSchemaFormat`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_strict(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, |obj| match &obj.strict {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `strict` field on a `JsonSchemaFormat` is `Some`.
///
/// `literllm_json_schema_format_strict` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_json_schema_format_has_strict(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::JsonSchemaFormat, _>(handle, |obj| obj.strict.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `LlmBudgetConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_budget_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmBudgetConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmBudgetConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmBudgetConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmBudgetConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmBudgetConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `global_limit` field from a `LlmBudgetConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_global_limit(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::LlmBudgetConfig, _>(handle, |obj| match &obj.global_limit {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `global_limit` field on a `LlmBudgetConfig` is `Some`.
///
/// `literllm_llm_budget_config_global_limit` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_has_global_limit(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::LlmBudgetConfig, _>(handle, |obj| obj.global_limit.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `model_limits` field from a `LlmBudgetConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_model_limits(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmBudgetConfig, _>(handle, |obj| match &obj.model_limits {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `enforcement` field from a `LlmBudgetConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_budget_config_enforcement(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmBudgetConfig, _>(handle, |obj| match &obj.enforcement {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmCacheConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_cache_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmCacheConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmCacheConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmCacheConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmCacheConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `max_entries` field from a `LlmCacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_max_entries(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| match &obj.max_entries {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_entries` field on a `LlmCacheConfig` is `Some`.
///
/// `literllm_llm_cache_config_max_entries` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_has_max_entries(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| obj.max_entries.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `ttl_seconds` field from a `LlmCacheConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_ttl_seconds(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| match &obj.ttl_seconds {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `ttl_seconds` field on a `LlmCacheConfig` is `Some`.
///
/// `literllm_llm_cache_config_ttl_seconds` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_has_ttl_seconds(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| obj.ttl_seconds.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `backend` field from a `LlmCacheConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_backend(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| match &obj.backend {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `backend_config` field from a `LlmCacheConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_cache_config_backend_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmCacheConfig, _>(handle, |obj| match &obj.backend_config {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `api_key` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_api_key(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.api_key {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.base_url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `timeout_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_timeout_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.timeout_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `timeout_secs` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_timeout_secs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_timeout_secs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.timeout_secs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_retries` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_max_retries(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.max_retries {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_retries` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_max_retries` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_max_retries(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.max_retries.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `temperature` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_temperature(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.temperature {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `temperature` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_temperature` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_temperature(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.temperature.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_tokens` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_max_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.max_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_tokens` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_max_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_max_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.max_tokens.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `load_env` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_load_env(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.load_env {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `load_env` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_load_env` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_load_env(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.load_env.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `headers` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_headers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.headers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `providers` field from a `LlmConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_providers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.providers {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `cache` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_llm_cache_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_cache(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.cache {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `budget` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_llm_budget_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_budget(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.budget {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `rate_limit` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_llm_rate_limit_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_rate_limit(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.rate_limit {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `in_flight_limit` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_llm_in_flight_limit_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_in_flight_limit(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.in_flight_limit {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `cost_tracking` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_cost_tracking(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.cost_tracking {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `cost_tracking` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_cost_tracking` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_cost_tracking(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.cost_tracking.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `tracing` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_tracing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.tracing {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `tracing` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_tracing` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_tracing(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.tracing.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `cooldown_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_cooldown_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.cooldown_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `cooldown_secs` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_cooldown_secs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_cooldown_secs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.cooldown_secs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `health_check_secs` field from a `LlmConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_health_check_secs(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.health_check_secs {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `health_check_secs` field on a `LlmConfig` is `Some`.
///
/// `literllm_llm_config_health_check_secs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_has_health_check_secs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| obj.health_check_secs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `bedrock` field from a `LlmConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_bedrock_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_config_bedrock(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmConfig, _>(handle, |obj| match &obj.bedrock {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `LlmInFlightLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_in_flight_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_in_flight_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmInFlightLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmInFlightLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_in_flight_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmInFlightLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmInFlightLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_in_flight_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmInFlightLimitConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `max_in_flight` field from a `LlmInFlightLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_in_flight_limit_config_max_in_flight(handle: AlefHandle) -> usize {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::client::LlmInFlightLimitConfig, _>(handle, |obj| match &obj.max_in_flight {
                Some(val) => *val,
                None => 0,
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_in_flight` field on a `LlmInFlightLimitConfig` is `Some`.
///
/// `literllm_llm_in_flight_limit_config_max_in_flight` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_in_flight_limit_config_has_max_in_flight(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmInFlightLimitConfig, _>(handle, |obj| {
            obj.max_in_flight.is_some() as i32
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `LlmProviderConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_provider_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmProviderConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmProviderConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmProviderConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmProviderConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmProviderConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmProviderConfig, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmProviderConfig, _>(handle, |obj| {
            match CString::new(obj.base_url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `auth_header` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_auth_header(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::client::LlmProviderConfig, _>(handle, |obj| match &obj.auth_header {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model_prefixes` field from a `LlmProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_provider_config_model_prefixes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::client::LlmProviderConfig, _>(handle, |obj| {
                match serde_json::to_string(&obj.model_prefixes) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `LlmRateLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_llm_rate_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::LlmRateLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `LlmRateLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `LlmRateLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::LlmRateLimitConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `rpm` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_rpm(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| match &obj.rpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `rpm` field on a `LlmRateLimitConfig` is `Some`.
///
/// `literllm_llm_rate_limit_config_rpm` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_has_rpm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| obj.rpm.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `tpm` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_tpm(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| match &obj.tpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `tpm` field on a `LlmRateLimitConfig` is `Some`.
///
/// `literllm_llm_rate_limit_config_tpm` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_has_tpm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| obj.tpm.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `window_seconds` field from a `LlmRateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_window_seconds(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| match &obj.window_seconds {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `window_seconds` field on a `LlmRateLimitConfig` is `Some`.
///
/// `literllm_llm_rate_limit_config_window_seconds` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_llm_rate_limit_config_has_window_seconds(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::LlmRateLimitConfig, _>(handle, |obj| obj.window_seconds.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `ModelInfo` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_model_info_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::ModelInfo>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModelInfo` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::ModelInfo, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModelInfo` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::ModelInfo>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `input_cost_per_token` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_input_cost_per_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.input_cost_per_token);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `output_cost_per_token` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_output_cost_per_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.output_cost_per_token);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `cache_read_input_token_cost` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_cache_read_input_token_cost(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.cache_read_input_token_cost {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `cache_read_input_token_cost` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_cache_read_input_token_cost` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_cache_read_input_token_cost(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.cache_read_input_token_cost.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `cache_creation_input_token_cost` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_cache_creation_input_token_cost(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.cache_creation_input_token_cost {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `cache_creation_input_token_cost` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_cache_creation_input_token_cost` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_cache_creation_input_token_cost(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.cache_creation_input_token_cost.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `input_cost_per_audio_token` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_input_cost_per_audio_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.input_cost_per_audio_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `input_cost_per_audio_token` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_input_cost_per_audio_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_input_cost_per_audio_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.input_cost_per_audio_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `output_cost_per_audio_token` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_output_cost_per_audio_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.output_cost_per_audio_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `output_cost_per_audio_token` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_output_cost_per_audio_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_output_cost_per_audio_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.output_cost_per_audio_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `output_cost_per_reasoning_token` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_output_cost_per_reasoning_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.output_cost_per_reasoning_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `output_cost_per_reasoning_token` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_output_cost_per_reasoning_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_output_cost_per_reasoning_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.output_cost_per_reasoning_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_tokens` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_max_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.max_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_tokens` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_max_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_max_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.max_tokens.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_input_tokens` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_max_input_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.max_input_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_input_tokens` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_max_input_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_max_input_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.max_input_tokens.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `max_output_tokens` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_max_output_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.max_output_tokens {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_output_tokens` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_max_output_tokens` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_max_output_tokens(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.max_output_tokens.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `mode` field from a `ModelInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_mode(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.mode {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `supports_vision` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_vision(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_vision {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_vision` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_vision` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_vision(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_vision.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_function_calling` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_function_calling(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_function_calling {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_function_calling` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_function_calling` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_function_calling(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_function_calling.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_reasoning` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_reasoning(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_reasoning {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_reasoning` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_reasoning` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_reasoning(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_reasoning.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_structured_output` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_structured_output(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_structured_output {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_structured_output` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_structured_output` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_structured_output(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_structured_output.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_audio_input` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_audio_input(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_audio_input {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_audio_input` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_audio_input` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_audio_input(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_audio_input.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_audio_output` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_audio_output(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_audio_output {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_audio_output` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_audio_output` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_audio_output(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_audio_output.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `supports_prompt_caching` field from a `ModelInfo`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_supports_prompt_caching(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match &obj.supports_prompt_caching {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `supports_prompt_caching` field on a `ModelInfo` is `Some`.
///
/// `literllm_model_info_supports_prompt_caching` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_has_supports_prompt_caching(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| obj.supports_prompt_caching.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `tiers` field from a `ModelInfo`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info_tiers(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::ModelInfo, _>(handle, |obj| match serde_json::to_string(&obj.tiers) {
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI JSON value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            Err(_) => {
                set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                std::ptr::null_mut()
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ModelObject` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_model_object_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModelObject>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModelObject` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModelObject, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModelObject` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModelObject>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ModelObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ModelObject, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `ModelObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ModelObject, _>(handle, |obj| match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created` field from a `ModelObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_created(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModelObject, _>(handle, |obj| obj.created);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `owned_by` field from a `ModelObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_object_owned_by(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ModelObject, _>(handle, |obj| {
            match CString::new(obj.owned_by.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ModelTier` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_model_tier_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::ModelTier>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModelTier` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::ModelTier, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModelTier` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::ModelTier>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `min_context_tokens` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_min_context_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.min_context_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `input_cost_per_token` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_input_cost_per_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.input_cost_per_token);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `output_cost_per_token` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_output_cost_per_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.output_cost_per_token);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `cache_read_input_token_cost` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_cache_read_input_token_cost(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| match &obj.cache_read_input_token_cost {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `cache_read_input_token_cost` field on a `ModelTier` is `Some`.
///
/// `literllm_model_tier_cache_read_input_token_cost` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_has_cache_read_input_token_cost(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.cache_read_input_token_cost.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `cache_creation_input_token_cost` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_cache_creation_input_token_cost(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| match &obj.cache_creation_input_token_cost {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `cache_creation_input_token_cost` field on a `ModelTier` is `Some`.
///
/// `literllm_model_tier_cache_creation_input_token_cost` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_has_cache_creation_input_token_cost(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.cache_creation_input_token_cost.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `input_cost_per_audio_token` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_input_cost_per_audio_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| match &obj.input_cost_per_audio_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `input_cost_per_audio_token` field on a `ModelTier` is `Some`.
///
/// `literllm_model_tier_input_cost_per_audio_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_has_input_cost_per_audio_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.input_cost_per_audio_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `output_cost_per_audio_token` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_output_cost_per_audio_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| match &obj.output_cost_per_audio_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `output_cost_per_audio_token` field on a `ModelTier` is `Some`.
///
/// `literllm_model_tier_output_cost_per_audio_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_has_output_cost_per_audio_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.output_cost_per_audio_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `output_cost_per_reasoning_token` field from a `ModelTier`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_output_cost_per_reasoning_token(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::ModelTier, _>(handle, |obj| match &obj.output_cost_per_reasoning_token {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `output_cost_per_reasoning_token` field on a `ModelTier` is `Some`.
///
/// `literllm_model_tier_output_cost_per_reasoning_token` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_tier_has_output_cost_per_reasoning_token(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::ModelTier, _>(handle, |obj| obj.output_cost_per_reasoning_token.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `ModelsListResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_models_list_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_models_list_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModelsListResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModelsListResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_models_list_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModelsListResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModelsListResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_models_list_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModelsListResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `object` field from a `ModelsListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_models_list_response_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ModelsListResponse, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `data` field from a `ModelsListResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_models_list_response_data(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ModelsListResponse, _>(handle, |obj| {
                match serde_json::to_string(&obj.data) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ModerationCategories` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_moderation_categories_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModerationCategories>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModerationCategories` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationCategories, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationCategories` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationCategories>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `sexual` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_sexual(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.sexual as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hate` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_hate(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.hate as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `harassment` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_harassment(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.harassment as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `self_harm` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_self_harm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.self_harm as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `sexual_minors` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_sexual_minors(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.sexual_minors as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `hate_threatening` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_hate_threatening(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.hate_threatening as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `violence_graphic` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_violence_graphic(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.violence_graphic as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `self_harm_intent` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_self_harm_intent(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.self_harm_intent as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `self_harm_instructions` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_self_harm_instructions(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.self_harm_instructions as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `harassment_threatening` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_harassment_threatening(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.harassment_threatening as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `violence` field from a `ModerationCategories`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_categories_violence(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategories, _>(handle, |obj| obj.violence as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ModerationCategoryScores` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_moderation_category_scores_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModerationCategoryScores>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModerationCategoryScores` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationCategoryScores` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationCategoryScores>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `sexual` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_sexual(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.sexual);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `hate` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_hate(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.hate);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `harassment` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_harassment(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.harassment);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `self_harm` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_self_harm(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.self_harm);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `sexual_minors` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_sexual_minors(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.sexual_minors);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `hate_threatening` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_hate_threatening(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.hate_threatening);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `violence_graphic` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_violence_graphic(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.violence_graphic);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `self_harm_intent` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_self_harm_intent(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.self_harm_intent);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `self_harm_instructions` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_self_harm_instructions(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.self_harm_instructions);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `harassment_threatening` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_harassment_threatening(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result =
            with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.harassment_threatening);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `violence` field from a `ModerationCategoryScores`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_category_scores_violence(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::ModerationCategoryScores, _>(handle, |obj| obj.violence);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Create a `ModerationRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_moderation_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModerationRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModerationRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `input` field from a `ModerationRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_moderation_input_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_request_input(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationRequest, _>(handle, |obj| {
            match insert_handle(obj.input.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `model` field from a `ModerationRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ModerationRequest, _>(handle, |obj| match &obj.model {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ModerationResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_moderation_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModerationResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModerationResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ModerationResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ModerationResponse, _>(handle, |obj| {
            match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `ModerationResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ModerationResponse, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `results` field from a `ModerationResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_response_results(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ModerationResponse, _>(handle, |obj| {
                match serde_json::to_string(&obj.results) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ModerationResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_moderation_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ModerationResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ModerationResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `flagged` field from a `ModerationResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_flagged(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationResult, _>(handle, |obj| obj.flagged as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `categories` field from a `ModerationResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_moderation_categories_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_categories(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationResult, _>(handle, |obj| {
            match insert_handle(obj.categories.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `category_scores` field from a `ModerationResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_moderation_category_scores_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_result_category_scores(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ModerationResult, _>(handle, |obj| {
            match insert_handle(obj.category_scores.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `OcrImage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_ocr_image_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_image_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::OcrImage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrImage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_image_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::OcrImage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrImage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_image_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::OcrImage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `OcrImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_image_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::OcrImage, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `image_base64` field from a `OcrImage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_image_image_base64(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::OcrImage, _>(handle, |obj| match &obj.image_base64 {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `OcrPage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_ocr_page_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::OcrPage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrPage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::OcrPage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrPage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::OcrPage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `index` field from a `OcrPage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::OcrPage, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `markdown` field from a `OcrPage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_markdown(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::OcrPage, _>(handle, |obj| match CString::new(obj.markdown.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `images` field from a `OcrPage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_images(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::OcrPage, _>(handle, |obj| match &obj.images {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `dimensions` field from a `OcrPage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_page_dimensions_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_page_dimensions(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::OcrPage, _>(handle, |obj| match &obj.dimensions {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `OcrRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_ocr_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::OcrRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::OcrRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::OcrRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `OcrRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::OcrRequest, _>(handle, |obj| match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `document` field from a `OcrRequest`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_ocr_document_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_document(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::OcrRequest, _>(handle, |obj| match insert_handle(obj.document.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `pages` field from a `OcrRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::OcrRequest, _>(handle, |obj| match &obj.pages {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `include_image_base64` field from a `OcrRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_include_image_base64(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::OcrRequest, _>(handle, |obj| match &obj.include_image_base64 {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `include_image_base64` field on a `OcrRequest` is `Some`.
///
/// `literllm_ocr_request_include_image_base64` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_request_has_include_image_base64(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::OcrRequest, _>(handle, |obj| obj.include_image_base64.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `OcrResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_ocr_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::OcrResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `OcrResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::OcrResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::OcrResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `pages` field from a `OcrResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_pages(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::OcrResponse, _>(handle, |obj| match serde_json::to_string(&obj.pages) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `OcrResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::OcrResponse, _>(handle, |obj| match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `usage` field from a `OcrResponse`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_usage_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_response_usage(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::OcrResponse, _>(handle, |obj| match &obj.usage {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PageDimensions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_page_dimensions_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_page_dimensions_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::PageDimensions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PageDimensions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_page_dimensions_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::PageDimensions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PageDimensions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_page_dimensions_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::PageDimensions>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `width` field from a `PageDimensions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_page_dimensions_width(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::PageDimensions, _>(handle, |obj| obj.width);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `height` field from a `PageDimensions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_page_dimensions_height(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::PageDimensions, _>(handle, |obj| obj.height);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `PromptTokensDetails` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_prompt_tokens_details_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_prompt_tokens_details_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::PromptTokensDetails>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `PromptTokensDetails` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_prompt_tokens_details_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::PromptTokensDetails, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `PromptTokensDetails` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_prompt_tokens_details_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::PromptTokensDetails>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `cached_tokens` field from a `PromptTokensDetails`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_prompt_tokens_details_cached_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::PromptTokensDetails, _>(handle, |obj| obj.cached_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `audio_tokens` field from a `PromptTokensDetails`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_prompt_tokens_details_audio_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::PromptTokensDetails, _>(handle, |obj| obj.audio_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ProviderCapabilities` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_provider_capabilities_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::provider::ProviderCapabilities>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProviderCapabilities` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProviderCapabilities` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::ProviderCapabilities>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `vision` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_vision(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.vision as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `reasoning` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_reasoning(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.reasoning as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `structured_output` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_structured_output(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.structured_output as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `function_calling` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_function_calling(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.function_calling as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `audio_in` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_audio_in(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.audio_in as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `audio_out` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_audio_out(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.audio_out as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `video_in` field from a `ProviderCapabilities`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_capabilities_video_in(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderCapabilities, _>(handle, |obj| obj.video_in as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ProviderConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_provider_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::provider::ProviderConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ProviderConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::ProviderConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ProviderConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::ProviderConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `display_name` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_display_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.display_name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `base_url` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_base_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.base_url {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `auth` field from a `ProviderConfig`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_auth_config_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_auth(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.auth {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `endpoints` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_endpoints(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.endpoints {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model_prefixes` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_model_prefixes(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.model_prefixes {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `param_mappings` field from a `ProviderConfig`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_provider_config_param_mappings(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::provider::ProviderConfig, _>(handle, |obj| match &obj.param_mappings {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Create a `RateLimitConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_rate_limit_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::RateLimitConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `RateLimitConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::RateLimitConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `RateLimitConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::RateLimitConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `rpm` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_rpm(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::RateLimitConfig, _>(handle, |obj| match &obj.rpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Report whether the `rpm` field on a `RateLimitConfig` is `Some`.
///
/// `literllm_rate_limit_config_rpm` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_has_rpm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::RateLimitConfig, _>(handle, |obj| obj.rpm.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `tpm` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_tpm(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::RateLimitConfig, _>(handle, |obj| match &obj.tpm {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Report whether the `tpm` field on a `RateLimitConfig` is `Some`.
///
/// `literllm_rate_limit_config_tpm` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_has_tpm(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::RateLimitConfig, _>(handle, |obj| obj.tpm.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Get the `window` field from a `RateLimitConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_window(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::RateLimitConfig, _>(handle, |obj| obj.window.as_millis() as u64);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tower")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rate_limit_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = liter_llm::RateLimitConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a `RerankRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_rerank_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::RerankRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RerankRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::RerankRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RerankRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::RerankRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `RerankRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `query` field from a `RerankRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_query(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| {
            match CString::new(obj.query.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `documents` field from a `RerankRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_documents(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| {
            match serde_json::to_string(&obj.documents) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `top_n` field from a `RerankRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_top_n(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| match &obj.top_n {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `top_n` field on a `RerankRequest` is `Some`.
///
/// `literllm_rerank_request_top_n` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_has_top_n(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| obj.top_n.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `return_documents` field from a `RerankRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_return_documents(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| match &obj.return_documents {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `return_documents` field on a `RerankRequest` is `Some`.
///
/// `literllm_rerank_request_return_documents` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_request_has_return_documents(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::RerankRequest, _>(handle, |obj| obj.return_documents.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `RerankResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_rerank_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::RerankResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RerankResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::RerankResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RerankResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::RerankResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `RerankResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankResponse, _>(handle, |obj| match &obj.id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `results` field from a `RerankResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_results(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.results) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `meta` field from a `RerankResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_response_meta(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankResponse, _>(handle, |obj| match &obj.meta {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `RerankResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_rerank_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::RerankResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RerankResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::RerankResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RerankResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::RerankResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `index` field from a `RerankResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::RerankResult, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `relevance_score` field from a `RerankResult`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_relevance_score(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::RerankResult, _>(handle, |obj| obj.relevance_score);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `document` field from a `RerankResult`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_rerank_result_document_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_document(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::RerankResult, _>(handle, |obj| match &obj.document {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `RerankResultDocument` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_rerank_result_document_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_document_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::RerankResultDocument>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `RerankResultDocument` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_document_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::RerankResultDocument, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RerankResultDocument` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_document_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::RerankResultDocument>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `text` field from a `RerankResultDocument`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_result_document_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::RerankResultDocument, _>(handle, |obj| {
            match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ResponseObject` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_response_object_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ResponseObject>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ResponseObject` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ResponseObject, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ResponseObject` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ResponseObject>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `object` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_object(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| {
            match CString::new(obj.object.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `created_at` field from a `ResponseObject`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_created_at(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| obj.created_at);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `model` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `status` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_status(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| {
            match CString::new(obj.status.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `output` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_output(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| {
            match serde_json::to_string(&obj.output) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `usage` field from a `ResponseObject`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_response_usage_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_usage(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| match &obj.usage {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `error` field from a `ResponseObject`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_object_error(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseObject, _>(handle, |obj| match &obj.error {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ResponseOutputItem` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_response_output_item_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_output_item_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ResponseOutputItem>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ResponseOutputItem` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_output_item_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ResponseOutputItem, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ResponseOutputItem` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_output_item_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ResponseOutputItem>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `item_type` field from a `ResponseOutputItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_output_item_item_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseOutputItem, _>(handle, |obj| {
            match CString::new(obj.item_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `ResponseOutputItem`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_output_item_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ResponseOutputItem, _>(handle, |obj| {
                match serde_json::to_string(&obj.content) {
                    Ok(s) => match CString::new(s) {
                        Ok(cs) => cs.into_raw(),
                        Err(_) => {
                            set_last_error(
                                ALEF_FFI_CONVERSION_ERROR,
                                "FFI JSON value contains an interior NUL byte",
                            );
                            std::ptr::null_mut()
                        }
                    },
                    Err(_) => {
                        set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                        std::ptr::null_mut()
                    }
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ResponseTool` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_response_tool_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_tool_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ResponseTool>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ResponseTool` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_tool_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ResponseTool, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ResponseTool` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_tool_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ResponseTool>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `tool_type` field from a `ResponseTool`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_tool_tool_type(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ResponseTool, _>(handle, |obj| {
            match CString::new(obj.tool_type.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `config` field from a `ResponseTool`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_tool_config(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ResponseTool, _>(handle, |obj| match serde_json::to_string(&obj.config) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ResponseUsage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_response_usage_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ResponseUsage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ResponseUsage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ResponseUsage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ResponseUsage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ResponseUsage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `input_tokens` field from a `ResponseUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_input_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ResponseUsage, _>(handle, |obj| obj.input_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `output_tokens` field from a `ResponseUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_output_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ResponseUsage, _>(handle, |obj| obj.output_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_tokens` field from a `ResponseUsage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_usage_total_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::ResponseUsage, _>(handle, |obj| obj.total_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SearchRequest` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_search_request_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SearchRequest>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SearchRequest` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SearchRequest, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SearchRequest` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SearchRequest>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `model` field from a `SearchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `query` field from a `SearchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_query(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| {
            match CString::new(obj.query.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `max_results` field from a `SearchRequest`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_max_results(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| match &obj.max_results {
            Some(val) => *val,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `max_results` field on a `SearchRequest` is `Some`.
///
/// `literllm_search_request_max_results` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_has_max_results(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| obj.max_results.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `search_domain_filter` field from a `SearchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_search_domain_filter(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| match &obj.search_domain_filter {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `country` field from a `SearchRequest`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_request_country(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchRequest, _>(handle, |obj| match &obj.country {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SearchResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_search_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SearchResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SearchResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SearchResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SearchResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SearchResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `results` field from a `SearchResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_response_results(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchResponse, _>(handle, |obj| {
            match serde_json::to_string(&obj.results) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `model` field from a `SearchResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_response_model(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchResponse, _>(handle, |obj| {
            match CString::new(obj.model.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SearchResult` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_search_result_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SearchResult>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SearchResult` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SearchResult, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SearchResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SearchResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `title` field from a `SearchResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_title(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::SearchResult, _>(handle, |obj| match CString::new(obj.title.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `url` field from a `SearchResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_url(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::SearchResult, _>(handle, |obj| match CString::new(obj.url.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `snippet` field from a `SearchResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_snippet(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchResult, _>(handle, |obj| {
            match CString::new(obj.snippet.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `date` field from a `SearchResult`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_search_result_date(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SearchResult, _>(handle, |obj| match &obj.date {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `SingleflightResult` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_singleflight_result_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::tower::SingleflightResult>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Create a `SpecificFunction` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_specific_function_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_function_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SpecificFunction>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SpecificFunction` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_function_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SpecificFunction, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SpecificFunction` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_function_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SpecificFunction>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `SpecificFunction`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_function_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SpecificFunction, _>(handle, |obj| {
            match CString::new(obj.name.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `SpecificToolChoice` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_specific_tool_choice_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_tool_choice_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SpecificToolChoice>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SpecificToolChoice` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_tool_choice_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SpecificToolChoice, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SpecificToolChoice` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_tool_choice_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SpecificToolChoice>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `choice_type` field from a `SpecificToolChoice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_tool_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_tool_choice_choice_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::SpecificToolChoice, _>(handle, |obj| {
            match insert_handle(obj.choice_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `function` field from a `SpecificToolChoice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_specific_function_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_specific_tool_choice_function(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::SpecificToolChoice, _>(handle, |obj| {
            match insert_handle(obj.function.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `StreamChoice` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_stream_choice_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::StreamChoice>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StreamChoice` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StreamChoice, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StreamChoice` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StreamChoice>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `index` field from a `StreamChoice`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamChoice, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `delta` field from a `StreamChoice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_stream_delta_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_delta(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::StreamChoice, _>(handle, |obj| match insert_handle(obj.delta.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `finish_reason` field from a `StreamChoice`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_finish_reason_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_choice_finish_reason(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamChoice, _>(handle, |obj| match &obj.finish_reason {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `StreamDelta` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_stream_delta_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::StreamDelta>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StreamDelta` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StreamDelta, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StreamDelta` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StreamDelta>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `role` field from a `StreamDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_role(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.role {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `content` field from a `StreamDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `tool_calls` field from a `StreamDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_tool_calls(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.tool_calls {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `function_call` field from a `StreamDelta`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_stream_function_call_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_function_call(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.function_call {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `refusal` field from a `StreamDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_refusal(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.refusal {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `reasoning_content` field from a `StreamDelta`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_delta_reasoning_content(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamDelta, _>(handle, |obj| match &obj.reasoning_content {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `StreamFunctionCall` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_stream_function_call_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_function_call_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::StreamFunctionCall>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StreamFunctionCall` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_function_call_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StreamFunctionCall, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StreamFunctionCall` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_function_call_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StreamFunctionCall>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `name` field from a `StreamFunctionCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_function_call_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamFunctionCall, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `arguments` field from a `StreamFunctionCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_function_call_arguments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamFunctionCall, _>(handle, |obj| match &obj.arguments {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `StreamOptions` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_stream_options_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_options_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::StreamOptions>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StreamOptions` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_options_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StreamOptions, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StreamOptions` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_options_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StreamOptions>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `include_usage` field from a `StreamOptions`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_options_include_usage(handle: AlefHandle) -> i32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamOptions, _>(handle, |obj| match &obj.include_usage {
            Some(val) => *val as i32,
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Report whether the `include_usage` field on a `StreamOptions` is `Some`.
///
/// `literllm_stream_options_include_usage` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_options_has_include_usage(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::StreamOptions, _>(handle, |obj| obj.include_usage.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Create a `StreamToolCall` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_stream_tool_call_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::StreamToolCall>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `StreamToolCall` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StreamToolCall, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StreamToolCall` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StreamToolCall>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `index` field from a `StreamToolCall`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_index(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamToolCall, _>(handle, |obj| obj.index);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `id` field from a `StreamToolCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::StreamToolCall, _>(handle, |obj| match &obj.id {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `call_type` field from a `StreamToolCall`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_tool_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_call_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamToolCall, _>(handle, |obj| match &obj.call_type {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `function` field from a `StreamToolCall`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_stream_function_call_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_tool_call_function(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::StreamToolCall, _>(handle, |obj| match &obj.function {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `SystemMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_system_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_system_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::SystemMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `SystemMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_system_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::SystemMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `SystemMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_system_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::SystemMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `SystemMessage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_user_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_system_message_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::SystemMessage, _>(handle, |obj| match insert_handle(obj.content.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `name` field from a `SystemMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_system_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::SystemMessage, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `ToolCall` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_tool_call_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ToolCall>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ToolCall` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ToolCall, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ToolCall` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ToolCall>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `ToolCall`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result =
            with_handle::<liter_llm::types::ToolCall, _>(handle, |obj| match CString::new(obj.id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `call_type` field from a `ToolCall`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_tool_type_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_call_type(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ToolCall, _>(handle, |obj| match insert_handle(obj.call_type.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `function` field from a `ToolCall`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_function_call_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_call_function(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ToolCall, _>(handle, |obj| match insert_handle(obj.function.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `ToolMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_tool_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::ToolMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `ToolMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ToolMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ToolMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ToolMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `ToolMessage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_user_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::ToolMessage, _>(handle, |obj| match insert_handle(obj.content.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `tool_call_id` field from a `ToolMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_tool_call_id(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ToolMessage, _>(handle, |obj| {
            match CString::new(obj.tool_call_id.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `name` field from a `ToolMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::ToolMessage, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TranscriptionResponse` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_transcription_response_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::TranscriptionResponse>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TranscriptionResponse` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TranscriptionResponse` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::TranscriptionResponse>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `text` field from a `TranscriptionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, |obj| {
            match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `language` field from a `TranscriptionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_language(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, |obj| match &obj.language {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Get the `duration` field from a `TranscriptionResponse`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_duration(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, |obj| match &obj.duration {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Report whether the `duration` field on a `TranscriptionResponse` is `Some`.
///
/// `literllm_transcription_response_duration` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_has_duration(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, |obj| obj.duration.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// Get the `segments` field from a `TranscriptionResponse`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_response_segments(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::TranscriptionResponse, _>(handle, |obj| match &obj.segments {
            Some(val) => match serde_json::to_string(&val) {
                Ok(s) => match CString::new(s) {
                    Ok(cs) => cs.into_raw(),
                    Err(_) => {
                        set_last_error(
                            ALEF_FFI_CONVERSION_ERROR,
                            "FFI JSON value contains an interior NUL byte",
                        );
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_error(ALEF_FFI_CONVERSION_ERROR, "failed to serialize FFI field value");
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `TranscriptionSegment` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_transcription_segment_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::TranscriptionSegment>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `TranscriptionSegment` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::TranscriptionSegment, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `TranscriptionSegment` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::TranscriptionSegment>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `id` field from a `TranscriptionSegment`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_id(handle: AlefHandle) -> u32 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::TranscriptionSegment, _>(handle, |obj| obj.id);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `start` field from a `TranscriptionSegment`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_start(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::TranscriptionSegment, _>(handle, |obj| obj.start);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `end` field from a `TranscriptionSegment`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_end(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::types::TranscriptionSegment, _>(handle, |obj| obj.end);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

/// Get the `text` field from a `TranscriptionSegment`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_transcription_segment_text(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::TranscriptionSegment, _>(handle, |obj| {
            match CString::new(obj.text.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            }
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

/// Create a `Usage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_usage_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::Usage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `Usage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::Usage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `Usage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::Usage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `prompt_tokens` field from a `Usage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_prompt_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Usage, _>(handle, |obj| obj.prompt_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `completion_tokens` field from a `Usage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_completion_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Usage, _>(handle, |obj| obj.completion_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `total_tokens` field from a `Usage`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_total_tokens(handle: AlefHandle) -> u64 {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Usage, _>(handle, |obj| obj.total_tokens);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `prompt_tokens_details` field from a `Usage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_prompt_tokens_details_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_usage_prompt_tokens_details(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result = with_handle::<liter_llm::types::Usage, _>(handle, |obj| match &obj.prompt_tokens_details {
            Some(val) => match insert_handle(val.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Create a `UserMessage` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_user_message_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_message_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::types::UserMessage>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

/// Serialize a `UserMessage` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_message_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::UserMessage, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `UserMessage` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_message_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::UserMessage>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Get the `content` field from a `UserMessage`.
/// A non-null returned handle is owned by the caller.
/// It must be freed with `literllm_user_content_free`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_message_content(handle: AlefHandle) -> AlefHandle {
    catch_ffi_panic(0, || {
        if handle == 0 {
            return 0;
        }
        let result =
            with_handle::<liter_llm::types::UserMessage, _>(handle, |obj| match insert_handle(obj.content.clone()) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })
}

/// Get the `name` field from a `UserMessage`.
/// A non-null returned pointer is owned by the caller.
/// It must be freed with `literllm_free_string`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_message_name(handle: AlefHandle) -> *mut std::ffi::c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        if handle == 0 {
            return std::ptr::null_mut();
        }
        let result = with_handle::<liter_llm::types::UserMessage, _>(handle, |obj| match &obj.name {
            Some(val) => match CString::new(val.to_string()) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(
                        ALEF_FFI_CONVERSION_ERROR,
                        "FFI field value contains an interior NUL byte",
                    );
                    std::ptr::null_mut()
                }
            },
            None => std::ptr::null_mut(),
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Create a `WaitForBatchConfig` from a JSON string. Returns null on failure.
/// # Safety
/// JSON string must be valid UTF-8 and null-terminated.
/// Returned handle must be freed with `literllm_wait_for_batch_config_free`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_from_json(json: *const c_char) -> AlefHandle {
    catch_ffi_panic(0, || {
        clear_last_error();
        if json.is_null() {
            set_last_error(1, "Null pointer passed for JSON string");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let c_str = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in JSON string");
                return 0;
            }
        };
        match serde_json::from_str::<liter_llm::client::WaitForBatchConfig>(c_str) {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(2, &e.to_string());
                0
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Serialize a `WaitForBatchConfig` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Free a `WaitForBatchConfig` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::client::WaitForBatchConfig>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Get the `initial_interval_secs` field from a `WaitForBatchConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_initial_interval_secs(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, |obj| obj.initial_interval_secs);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Get the `max_interval_secs` field from a `WaitForBatchConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_max_interval_secs(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, |obj| obj.max_interval_secs);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Get the `backoff_multiplier` field from a `WaitForBatchConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_backoff_multiplier(handle: AlefHandle) -> f32 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, |obj| obj.backoff_multiplier);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Get the `timeout_secs` field from a `WaitForBatchConfig`.
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_timeout_secs(handle: AlefHandle) -> f64 {
    catch_ffi_panic(0.0, || {
        if handle == 0 {
            return 0.0;
        }
        let result = with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, |obj| match &obj.timeout_secs {
            Some(val) => *val,
            None => 0.0,
        });
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                0.0
            }
        }
    })
}

#[cfg(any(feature = "native-http", feature = "wasm-http"))]
/// Report whether the `timeout_secs` field on a `WaitForBatchConfig` is `Some`.
///
/// `literllm_wait_for_batch_config_timeout_secs` cannot distinguish a `None` field from a
/// legitimate zero-valued `Some` at the C ABI boundary -- there is no null representation for a
/// numeric return, so both collapse to the same sentinel. Call this function first: `1` means
/// the field getter's return value is meaningful, `0` means the field is absent and the getter's
/// sentinel must be ignored, `-1` reports an invalid handle (see `literllm_last_error_code`).
/// # Safety
/// Pointer must be a valid handle returned by this library.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_has_timeout_secs(handle: AlefHandle) -> i32 {
    catch_ffi_panic(-1, || {
        if handle == 0 {
            return -1;
        }
        let result =
            with_handle::<liter_llm::client::WaitForBatchConfig, _>(handle, |obj| obj.timeout_secs.is_some() as i32);
        match result {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                -1
            }
        }
    })
}

/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_wait_for_batch_config_default() -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let result = liter_llm::client::WaitForBatchConfig::default();
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Convert an integer to a `AssistantContent` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_content_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Parts
            _ => {
                set_last_error(1, "Invalid AssistantContent variant");
                -1
            }
        }
    })
}

/// Convert a `AssistantContent` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_content_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Text" => 0,
            "Parts" => 1,
            _ => {
                set_last_error(1, "Unknown AssistantContent variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AssistantPart` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_part_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Refusal
            2 => 2, // OutputImage
            3 => 3, // OutputAudio
            _ => {
                set_last_error(1, "Invalid AssistantPart variant");
                -1
            }
        }
    })
}

/// Convert a `AssistantPart` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_part_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "refusal" => 1,
            "output_image" => 2,
            "output_audio" => 3,
            _ => {
                set_last_error(1, "Unknown AssistantPart variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AuthHeaderFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_header_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Bearer
            1 => 1, // ApiKey
            2 => 2, // None
            _ => {
                set_last_error(1, "Invalid AuthHeaderFormat variant");
                -1
            }
        }
    })
}

/// Convert a `AuthHeaderFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_header_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Bearer" => 0,
            "ApiKey" => 1,
            "None" => 2,
            _ => {
                set_last_error(1, "Unknown AuthHeaderFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `AuthType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Bearer
            1 => 1, // ApiKey
            2 => 2, // None
            3 => 3, // Unknown
            _ => {
                set_last_error(1, "Invalid AuthType variant");
                -1
            }
        }
    })
}

/// Convert a `AuthType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "bearer" => 0,
            "api-key" => 1,
            "none" => 2,
            "unknown" => 3,
            _ => {
                set_last_error(1, "Unknown AuthType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `BatchStatus` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_status_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Validating
            1 => 1, // Failed
            2 => 2, // InProgress
            3 => 3, // Finalizing
            4 => 4, // Completed
            5 => 5, // Expired
            6 => 6, // Cancelling
            7 => 7, // Cancelled
            _ => {
                set_last_error(1, "Invalid BatchStatus variant");
                -1
            }
        }
    })
}

/// Convert a `BatchStatus` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_status_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "validating" => 0,
            "failed" => 1,
            "in_progress" => 2,
            "finalizing" => 3,
            "completed" => 4,
            "expired" => 5,
            "cancelling" => 6,
            "cancelled" => 7,
            _ => {
                set_last_error(1, "Unknown BatchStatus variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CacheBackend` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_backend_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Memory
            1 => 1, // OpenDal
            _ => {
                set_last_error(1, "Invalid CacheBackend variant");
                -1
            }
        }
    })
}

/// Convert a `CacheBackend` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_backend_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "memory" => 0,
            "open_dal" => 1,
            _ => {
                set_last_error(1, "Unknown CacheBackend variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `CircuitState` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_circuit_state_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Closed
            1 => 1, // Open
            2 => 2, // HalfOpen
            _ => {
                set_last_error(1, "Invalid CircuitState variant");
                -1
            }
        }
    })
}

/// Convert a `CircuitState` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_circuit_state_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Closed" => 0,
            "Open" => 1,
            "HalfOpen" => 2,
            _ => {
                set_last_error(1, "Unknown CircuitState variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ContentPart` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_content_part_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // ImageUrl
            2 => 2, // Document
            3 => 3, // InputAudio
            _ => {
                set_last_error(1, "Invalid ContentPart variant");
                -1
            }
        }
    })
}

/// Convert a `ContentPart` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_content_part_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "image_url" => 1,
            "document" => 2,
            "input_audio" => 3,
            _ => {
                set_last_error(1, "Unknown ContentPart variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EmbeddingContentPart` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_content_part_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // ImageUrl
            2 => 2, // ImageBase64
            _ => {
                set_last_error(1, "Invalid EmbeddingContentPart variant");
                -1
            }
        }
    })
}

/// Convert a `EmbeddingContentPart` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_content_part_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "image_url" => 1,
            "image_base64" => 2,
            _ => {
                set_last_error(1, "Unknown EmbeddingContentPart variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EmbeddingFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Float
            1 => 1, // Base64
            _ => {
                set_last_error(1, "Invalid EmbeddingFormat variant");
                -1
            }
        }
    })
}

/// Convert a `EmbeddingFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "float" => 0,
            "base64" => 1,
            _ => {
                set_last_error(1, "Unknown EmbeddingFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `EmbeddingInput` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_input_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Single
            1 => 1, // Multiple
            2 => 2, // Multimodal
            _ => {
                set_last_error(1, "Invalid EmbeddingInput variant");
                -1
            }
        }
    })
}

/// Convert a `EmbeddingInput` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_input_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Single" => 0,
            "Multiple" => 1,
            "Multimodal" => 2,
            _ => {
                set_last_error(1, "Unknown EmbeddingInput variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `Enforcement` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_enforcement_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Hard
            1 => 1, // Soft
            _ => {
                set_last_error(1, "Invalid Enforcement variant");
                -1
            }
        }
    })
}

/// Convert a `Enforcement` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_enforcement_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Hard" => 0,
            "Soft" => 1,
            _ => {
                set_last_error(1, "Unknown Enforcement variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `FilePurpose` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_purpose_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Assistants
            1 => 1, // Batch
            2 => 2, // FineTune
            3 => 3, // Vision
            _ => {
                set_last_error(1, "Invalid FilePurpose variant");
                -1
            }
        }
    })
}

/// Convert a `FilePurpose` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_purpose_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "assistants" => 0,
            "batch" => 1,
            "fine-tune" => 2,
            "vision" => 3,
            _ => {
                set_last_error(1, "Unknown FilePurpose variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `FinishReason` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_finish_reason_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Stop
            1 => 1, // Length
            2 => 2, // ToolCalls
            3 => 3, // ContentFilter
            4 => 4, // FunctionCall
            5 => 5, // Other
            _ => {
                set_last_error(1, "Invalid FinishReason variant");
                -1
            }
        }
    })
}

/// Convert a `FinishReason` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_finish_reason_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "stop" => 0,
            "length" => 1,
            "tool_calls" => 2,
            "content_filter" => 3,
            "function_call" => 4,
            "other" => 5,
            _ => {
                set_last_error(1, "Unknown FinishReason variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `HealthStatus` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_health_status_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Healthy
            1 => 1, // Unhealthy
            _ => {
                set_last_error(1, "Invalid HealthStatus variant");
                -1
            }
        }
    })
}

/// Convert a `HealthStatus` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_health_status_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Healthy" => 0,
            "Unhealthy" => 1,
            _ => {
                set_last_error(1, "Unknown HealthStatus variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ImageDetail` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_detail_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Low
            1 => 1, // High
            2 => 2, // Auto
            _ => {
                set_last_error(1, "Invalid ImageDetail variant");
                -1
            }
        }
    })
}

/// Convert a `ImageDetail` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_detail_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "low" => 0,
            "high" => 1,
            "auto" => 2,
            _ => {
                set_last_error(1, "Unknown ImageDetail variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `Message` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_message_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // System
            1 => 1, // User
            2 => 2, // Assistant
            3 => 3, // Tool
            4 => 4, // Developer
            5 => 5, // Function
            _ => {
                set_last_error(1, "Invalid Message variant");
                -1
            }
        }
    })
}

/// Convert a `Message` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_message_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "system" => 0,
            "user" => 1,
            "assistant" => 2,
            "tool" => 3,
            "developer" => 4,
            "function" => 5,
            _ => {
                set_last_error(1, "Unknown Message variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `Modality` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_modality_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Audio
            2 => 2, // Image
            _ => {
                set_last_error(1, "Invalid Modality variant");
                -1
            }
        }
    })
}

/// Convert a `Modality` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_modality_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "audio" => 1,
            "image" => 2,
            _ => {
                set_last_error(1, "Unknown Modality variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ModerationInput` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_input_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Single
            1 => 1, // Multiple
            _ => {
                set_last_error(1, "Invalid ModerationInput variant");
                -1
            }
        }
    })
}

/// Convert a `ModerationInput` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_input_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Single" => 0,
            "Multiple" => 1,
            _ => {
                set_last_error(1, "Unknown ModerationInput variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `OcrDocument` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_document_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Url
            1 => 1, // Base64
            _ => {
                set_last_error(1, "Invalid OcrDocument variant");
                -1
            }
        }
    })
}

/// Convert a `OcrDocument` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_document_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "document_url" => 0,
            "base64" => 1,
            _ => {
                set_last_error(1, "Unknown OcrDocument variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ReasoningEffort` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_reasoning_effort_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Low
            1 => 1, // Medium
            2 => 2, // High
            3 => 3, // Minimal
            4 => 4, // Max
            _ => {
                set_last_error(1, "Invalid ReasoningEffort variant");
                -1
            }
        }
    })
}

/// Convert a `ReasoningEffort` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_reasoning_effort_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "low" => 0,
            "medium" => 1,
            "high" => 2,
            "minimal" => 3,
            "max" => 4,
            _ => {
                set_last_error(1, "Unknown ReasoningEffort variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RefreshOutcome` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_outcome_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Disabled
            1 => 1, // FromCache
            2 => 2, // Fetched
            _ => {
                set_last_error(1, "Invalid RefreshOutcome variant");
                -1
            }
        }
    })
}

/// Convert a `RefreshOutcome` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_outcome_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Disabled" => 0,
            "FromCache" => 1,
            "Fetched" => 2,
            _ => {
                set_last_error(1, "Unknown RefreshOutcome variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `RerankDocument` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_document_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Object
            _ => {
                set_last_error(1, "Invalid RerankDocument variant");
                -1
            }
        }
    })
}

/// Convert a `RerankDocument` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_rerank_document_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Text" => 0,
            "Object" => 1,
            _ => {
                set_last_error(1, "Unknown RerankDocument variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ResponseFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // JsonObject
            2 => 2, // JsonSchema
            _ => {
                set_last_error(1, "Invalid ResponseFormat variant");
                -1
            }
        }
    })
}

/// Convert a `ResponseFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "text" => 0,
            "json_object" => 1,
            "json_schema" => 2,
            _ => {
                set_last_error(1, "Unknown ResponseFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `StopSequence` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stop_sequence_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Single
            1 => 1, // Multiple
            _ => {
                set_last_error(1, "Invalid StopSequence variant");
                -1
            }
        }
    })
}

/// Convert a `StopSequence` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stop_sequence_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Single" => 0,
            "Multiple" => 1,
            _ => {
                set_last_error(1, "Unknown StopSequence variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `StreamFormat` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_format_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Sse
            1 => 1, // AwsEventStream
            _ => {
                set_last_error(1, "Invalid StreamFormat variant");
                -1
            }
        }
    })
}

/// Convert a `StreamFormat` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stream_format_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "sse" => 0,
            "aws_event_stream" => 1,
            _ => {
                set_last_error(1, "Unknown StreamFormat variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ToolChoice` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Mode
            1 => 1, // Specific
            _ => {
                set_last_error(1, "Invalid ToolChoice variant");
                -1
            }
        }
    })
}

/// Convert a `ToolChoice` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Mode" => 0,
            "Specific" => 1,
            _ => {
                set_last_error(1, "Unknown ToolChoice variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ToolChoiceMode` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_mode_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Auto
            1 => 1, // Required
            2 => 2, // None
            _ => {
                set_last_error(1, "Invalid ToolChoiceMode variant");
                -1
            }
        }
    })
}

/// Convert a `ToolChoiceMode` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_mode_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "auto" => 0,
            "required" => 1,
            "none" => 2,
            _ => {
                set_last_error(1, "Unknown ToolChoiceMode variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `ToolType` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_type_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Function
            _ => {
                set_last_error(1, "Invalid ToolType variant");
                -1
            }
        }
    })
}

/// Convert a `ToolType` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_type_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "function" => 0,
            _ => {
                set_last_error(1, "Unknown ToolType variant");
                -1
            }
        }
    })
}

/// Convert an integer to a `UserContent` variant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure all pointer arguments are valid or null.
/// Returned pointers must be freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_content_from_i32(value: i32) -> i32 {
    catch_ffi_panic(-1, || {
        match value {
            0 => 0, // Text
            1 => 1, // Parts
            _ => {
                set_last_error(1, "Invalid UserContent variant");
                -1
            }
        }
    })
}

/// Convert a `UserContent` serde wire value (C string) to its integer discriminant. Returns -1 on invalid input.
/// # Safety
/// Caller must ensure `ptr` is a valid pointer to a `c_char` or null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_content_from_str(name: *const c_char) -> i32 {
    catch_ffi_panic(-1, || {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for enum name");
            return -1;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let s = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s,
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in enum name");
                return -1;
            }
        };
        match s {
            "Text" => 0,
            "Parts" => 1,
            _ => {
                set_last_error(1, "Unknown UserContent variant");
                -1
            }
        }
    })
}

/// Free a `AssistantContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::AssistantContent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `AssistantContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::AssistantContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `AssistantContent` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_assistant_content_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::AssistantContent, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "AssistantContent variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AuthHeaderFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_header_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::custom::AuthHeaderFormat>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `AuthHeaderFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_header_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::custom::AuthHeaderFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `AuthHeaderFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_header_format_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::provider::custom::AuthHeaderFormat, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "AuthHeaderFormat variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `AuthType` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_type_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::provider::AuthType>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `AuthType` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_type_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::provider::AuthType, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `AuthType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_auth_type_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::provider::AuthType, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "AuthType variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `BatchStatus` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_status_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::BatchStatus>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `BatchStatus` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_status_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::BatchStatus, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `BatchStatus` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_batch_status_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::BatchStatus, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "BatchStatus variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `CacheBackend` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_backend_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::CacheBackend>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `CacheBackend` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_backend_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::CacheBackend, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Render a `CacheBackend` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_cache_backend_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::CacheBackend, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "CacheBackend variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::EmbeddingFormat>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `EmbeddingFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::EmbeddingFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `EmbeddingFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_format_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::EmbeddingFormat, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "EmbeddingFormat variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `EmbeddingInput` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_input_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::EmbeddingInput>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `EmbeddingInput` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_input_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::EmbeddingInput, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `EmbeddingInput` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_embedding_input_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::EmbeddingInput, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "EmbeddingInput variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Free a `Enforcement` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_enforcement_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::Enforcement>(handle)
        {
            set_handle_error(&error);
        }
    })
}

#[cfg(feature = "tower")]
/// Serialize a `Enforcement` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_enforcement_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::Enforcement, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

#[cfg(feature = "tower")]
/// Render a `Enforcement` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_enforcement_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::Enforcement, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "Enforcement variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FilePurpose` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_purpose_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FilePurpose>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `FilePurpose` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_purpose_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FilePurpose, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `FilePurpose` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_file_purpose_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::FilePurpose, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "FilePurpose variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `FinishReason` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_finish_reason_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::FinishReason>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `FinishReason` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_finish_reason_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::FinishReason, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `FinishReason` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_finish_reason_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::FinishReason, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "FinishReason variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ImageDetail` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_detail_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ImageDetail>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ImageDetail` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_detail_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ImageDetail, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ImageDetail` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_image_detail_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ImageDetail, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ImageDetail variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ModerationInput` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_input_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ModerationInput>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ModerationInput` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_input_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ModerationInput, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ModerationInput` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_moderation_input_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ModerationInput, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ModerationInput variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `OcrDocument` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_document_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::OcrDocument>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `OcrDocument` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_document_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::OcrDocument, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `OcrDocument` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ocr_document_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::OcrDocument, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "OcrDocument variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ReasoningEffort` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_reasoning_effort_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ReasoningEffort>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ReasoningEffort` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_reasoning_effort_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ReasoningEffort, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ReasoningEffort` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_reasoning_effort_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ReasoningEffort, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ReasoningEffort variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `RefreshOutcome` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_outcome_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::cost::refresh::RefreshOutcome>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `RefreshOutcome` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_outcome_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::cost::refresh::RefreshOutcome, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `RefreshOutcome` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_outcome_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::cost::refresh::RefreshOutcome, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "RefreshOutcome variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ResponseFormat` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_format_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ResponseFormat>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ResponseFormat` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_format_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ResponseFormat, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ResponseFormat` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_response_format_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ResponseFormat, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ResponseFormat variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `StopSequence` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stop_sequence_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::StopSequence>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `StopSequence` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stop_sequence_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::StopSequence, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `StopSequence` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_stop_sequence_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::StopSequence, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "StopSequence variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ToolChoice` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ToolChoice>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ToolChoice` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ToolChoice, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ToolChoice` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_choice_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ToolChoice, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ToolChoice variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `ToolType` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_type_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::ToolType>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `ToolType` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_type_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::ToolType, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `ToolType` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_tool_type_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::ToolType, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "ToolType variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Free a `UserContent` handle.
/// # Safety
/// Handle must have been returned by this library, or be zero.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_content_free(handle: AlefHandle) {
    catch_ffi_panic((), || {
        if handle != 0
            && let Err(error) = remove_handle::<liter_llm::types::UserContent>(handle)
        {
            set_handle_error(&error);
        }
    })
}

/// Serialize a `UserContent` to a JSON string. Returns null on failure.
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_content_to_json(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        match with_handle::<liter_llm::types::UserContent, _>(handle, serde_json::to_string) {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(Err(error)) => {
                set_last_error(2, &error.to_string());
                std::ptr::null_mut()
            }
            Ok(Ok(s)) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(e) => {
                    set_last_error(2, &e.to_string());
                    std::ptr::null_mut()
                }
            },
        }
    })
}

/// Render a `UserContent` as its string representation
/// (the unit-variant name as serialized by serde — e.g. `"completed"`,
/// without surrounding JSON quotes).
/// # Safety
/// `handle` must be a valid, non-zero handle returned by a `literllm` function.
/// The returned string must be freed with `literllm_free_string`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_user_content_to_string(handle: AlefHandle) -> *mut c_char {
    catch_ffi_panic(std::ptr::null_mut(), || {
        clear_last_error();
        if handle == 0 {
            set_last_error(ALEF_INVALID_HANDLE_ERROR, "zero is not a valid handle");
            return std::ptr::null_mut();
        }
        let rendered = with_handle::<liter_llm::types::UserContent, _>(handle, |val| {
            serde_json::to_value(val)
                .ok()
                .and_then(|v| v.as_str().map(str::to_owned))
                .unwrap_or_default()
        });
        match rendered {
            Err(error) => {
                set_handle_error(&error);
                std::ptr::null_mut()
            }
            Ok(s) => match CString::new(s) {
                Ok(cs) => cs.into_raw(),
                Err(_) => {
                    set_last_error(1, "UserContent variant contained interior NUL byte");
                    std::ptr::null_mut()
                }
            },
        }
    })
}

// 16 MiB: tokio's ~2 MB default worker stack can overflow on a deep extraction
// future (a nested archive member, a multi-stage OCR pipeline), and a stack overflow
// aborts the process with SIGBUS instead of raising a catchable panic.
const FFI_RUNTIME_STACK_SIZE_BYTES: usize = 16 * 1024 * 1024;

fn get_ffi_runtime() -> &'static tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RUNTIME: OnceLock<tokio::runtime::Runtime> = OnceLock::new();
    RUNTIME.get_or_init(|| {
        tokio::runtime::Builder::new_multi_thread()
            .enable_all()
            .thread_stack_size(FFI_RUNTIME_STACK_SIZE_BYTES)
            .build()
            .expect("Failed to create tokio runtime")
    })
}

/// Return all provider configs from the registry.
///
/// Useful for tooling, documentation generation, or runtime enumeration.
/// Returns the public `ProviderConfig` slice (without capability flags).
/// To query capability flags for a specific provider use `capabilities`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_all_providers() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("literllm_all_providers", 0);
        let result = liter_llm::provider::all_providers();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("literllm_all_providers", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("literllm_all_providers", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("literllm_all_providers", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `literllm_all_providers` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `literllm_all_providers`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_all_providers_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("literllm_all_providers"))
}

/// Return the capability flags for a named provider.
///
/// Performs an O(n) linear scan over the embedded registry (165 entries).
/// Returns an owned value so bindings can pass capability data without
/// borrowing registry internals.
///
/// For unknown `provider_name` values the function returns an all-`false`
/// sentinel so callers never need to handle `Option`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_capabilities(provider_name: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if provider_name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'provider_name'");
            return 0;
        }
        // SAFETY: null check above guarantees provider_name is a valid pointer; string is valid UTF-8 from caller.
        let provider_name_rs = match unsafe { CStr::from_ptr(provider_name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'provider_name'");
                return 0;
            }
        };

        let result = liter_llm::provider::capabilities(&provider_name_rs);
        match insert_handle(result) {
            Ok(handle) => handle,
            Err(error) => {
                set_handle_error(&error);
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Assert that `current_len + incoming` does not exceed `limit`.
///
/// Call this before appending `incoming` bytes to any buffer that must
/// stay below `limit`.  Returns `Err(LiterLlmError::Streaming)` on overflow
/// and emits a `tracing::warn!` with context.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// check_bound("SSE buffer", buffer.len(), chunk.len(), SSE_BUFFER_MAX_BYTES)?;
/// buffer.push_str(chunk_str);
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_check_bound(
    context: *const std::ffi::c_char,
    current_len: usize,
    incoming: usize,
    limit: usize,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if context.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'context'");
            return -1;
        }
        // SAFETY: null check above guarantees context is a valid pointer; string is valid UTF-8 from caller.
        let context_rs = match unsafe { CStr::from_ptr(context) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'context'");
                return -1;
            }
        };

        let current_len_rs = current_len;

        let incoming_rs = incoming;

        let limit_rs = limit;

        let result = liter_llm::util::bounds::check_bound(&context_rs, current_len_rs, incoming_rs, limit_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Remove all guardrails from the global registry.
///
/// Primarily useful in tests to reset state between test cases.
///
/// If the lock was poisoned by a panicking guardrail on a previous access,
/// the poisoned state is recovered rather than propagating the panic.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_clear() {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(liter_llm::guardrail::registry::clear)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Clear the overlay registry, reverting `completion_cost`,
/// `completion_cost_with_cache`, and `model_info` to the
/// embedded catalog.
///
/// Primarily a test seam (see `install_catalog_overlay_from_str`); also
/// usable by long-running processes that want to abandon a runtime refresh.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_clear_catalog_overlay() {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(
        liter_llm::cost::refresh::clear_catalog_overlay,
    )) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Calculate the estimated cost of a completion given a model name and token
/// counts.
///
/// Returns `None` if the model is not present in the embedded pricing registry.
/// Returns `Some(cost_usd)` otherwise, where the value is in US dollars.
///
/// When an exact model name match is not found, progressively shorter prefixes
/// are tried by stripping from the last `-` or `.` separator.  For example,
/// `gpt-4-0613` will match `gpt-4` if no `gpt-4-0613` entry exists.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use liter_llm::cost;
///
/// let usd = cost::completion_cost("gpt-4o", 1_000, 500).expect("gpt-4o is a known model");
/// // 1000 * 0.0000025 + 500 * 0.00001 = 0.0025 + 0.005 = 0.0075
/// assert!((usd - 0.0075).abs() < 1e-9);
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_completion_cost(
    model: *const std::ffi::c_char,
    prompt_tokens: u64,
    completion_tokens: u64,
) -> f64 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return 0.0;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return 0.0;
            }
        };

        let prompt_tokens_rs = prompt_tokens;

        let completion_tokens_rs = completion_tokens;

        let result = liter_llm::completion_cost(&model_rs, prompt_tokens_rs, completion_tokens_rs);
        result.unwrap_or(0.0)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0.0
        }
    }
}

/// Report whether `completion_cost` returned `Some`.
///
/// `literllm_completion_cost` cannot distinguish a `None` result from a legitimate zero-valued `Some`
/// at the C ABI boundary. Call this function first: `1` means the sibling getter's return value is
/// meaningful, `0` means the result was absent and the getter's sentinel must be ignored, `-1` reports
/// an invalid handle or a call error (see `literllm_last_error_code`).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_completion_cost_has_result(
    model: *const std::ffi::c_char,
    prompt_tokens: u64,
    completion_tokens: u64,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return -1;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return -1;
            }
        };

        let prompt_tokens_rs = prompt_tokens;

        let completion_tokens_rs = completion_tokens;

        let result = liter_llm::completion_cost(&model_rs, prompt_tokens_rs, completion_tokens_rs);
        i32::from(result.is_some())
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Calculate the estimated cost of a completion, accounting for cached
/// (cache-hit) prompt tokens billed at the provider's discounted rate.
///
/// `cached_tokens` is the count of prompt tokens served from the provider's
/// prompt cache. It must be `<= prompt_tokens` (cached tokens are a subset of
/// the prompt). The non-cached portion is billed at `input_cost_per_token`
/// and the cached portion at `cache_read_input_token_cost` when the model
/// has cache pricing; otherwise the entire prompt is billed at the regular
/// input rate.
///
/// Returns `None` if the model is not present in the embedded pricing
/// registry, mirroring `completion_cost`.
///
/// When the model has `ModelPricing.tiers`, the tier whose
/// `min_context_tokens` is the highest value `<= prompt_tokens` supplies the
/// input/output/cache rates for the whole call; models without tiers (or
/// when `prompt_tokens` is below every tier threshold) use the base rates
/// unchanged, matching the original flat-rate behaviour.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_completion_cost_with_cache(
    model: *const std::ffi::c_char,
    prompt_tokens: u64,
    cached_tokens: u64,
    completion_tokens: u64,
) -> f64 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return 0.0;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return 0.0;
            }
        };

        let prompt_tokens_rs = prompt_tokens;

        let cached_tokens_rs = cached_tokens;

        let completion_tokens_rs = completion_tokens;

        let result =
            liter_llm::completion_cost_with_cache(&model_rs, prompt_tokens_rs, cached_tokens_rs, completion_tokens_rs);
        result.unwrap_or(0.0)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0.0
        }
    }
}

/// Report whether `completion_cost_with_cache` returned `Some`.
///
/// `literllm_completion_cost_with_cache` cannot distinguish a `None` result from a legitimate
/// zero-valued `Some` at the C ABI boundary. Call this function first: `1` means the sibling getter's
/// return value is meaningful, `0` means the result was absent and the getter's sentinel must be
/// ignored, `-1` reports an invalid handle or a call error (see `literllm_last_error_code`).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_completion_cost_with_cache_has_result(
    model: *const std::ffi::c_char,
    prompt_tokens: u64,
    cached_tokens: u64,
    completion_tokens: u64,
) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return -1;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return -1;
            }
        };

        let prompt_tokens_rs = prompt_tokens;

        let cached_tokens_rs = cached_tokens;

        let completion_tokens_rs = completion_tokens;

        let result =
            liter_llm::completion_cost_with_cache(&model_rs, prompt_tokens_rs, cached_tokens_rs, completion_tokens_rs);
        i32::from(result.is_some())
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Return the set of complex provider names.
///
/// Complex providers require custom auth/routing logic beyond simple bearer
/// tokens (e.g. AWS Bedrock SigV4, Vertex AI OAuth2).
///
/// The returned reference points into the static registry â no allocation.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_complex_provider_names() -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("literllm_complex_provider_names", 0);
        let result = liter_llm::provider::complex_provider_names();
        match result {
            Ok(val) => match serde_json::to_string(&val) {
                Ok(__alef_return) => match CString::new(__alef_return) {
                    Ok(cs) => {
                        set_last_return_len("literllm_complex_provider_names", cs.as_bytes().len());
                        cs.into_raw()
                    }
                    Err(_) => {
                        set_last_return_len("literllm_complex_provider_names", 0);
                        std::ptr::null_mut()
                    }
                },
                Err(_) => {
                    set_last_return_len("literllm_complex_provider_names", 0);
                    std::ptr::null_mut()
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                std::ptr::null_mut()
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `literllm_complex_provider_names`
/// on this thread. Returns 0 when the primary call returned null or failed before producing a string.
/// Enables safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `literllm_complex_provider_names`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_complex_provider_names_len() -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("literllm_complex_provider_names"))
}

/// Count tokens for a full `ChatCompletionRequest`.
///
/// Sums tokens across all message text contents plus a per-message overhead
/// of ~4 tokens (for role, separators, and formatting metadata). Tool
/// definitions and multimodal content parts (images, audio, documents) are
/// not counted â only textual content contributes to the token total.
/// \note Returns `LiterLlmError.BadRequest` if the tokenizer cannot be loaded or
/// if tokenization fails for any message.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tokenizer")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_count_request_tokens(model: *const std::ffi::c_char, req: AlefHandle) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: req,
            expected_type: std::any::TypeId::of::<liter_llm::types::ChatCompletionRequest>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return 0;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return 0;
            }
        };

        if req == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'req'",
            );
            return 0;
        }
        let req_rs_ptr = match locked_handle_ptr::<liter_llm::types::ChatCompletionRequest>(&mut __alef_guards, req) {
            Ok(value) => value,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let req_rs = unsafe { &*req_rs_ptr };

        let result = liter_llm::count_request_tokens(&model_rs, &req_rs);
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Count tokens in a text string using the tokenizer for the given model.
///
/// The tokenizer is resolved from the model name prefix (e.g. `"gpt-4o"` maps
/// to the `Xenova/gpt-4o` HuggingFace tokenizer). Tokenizers are cached after
/// first load.
/// \note Returns `LiterLlmError.BadRequest` if the tokenizer cannot be loaded
/// (e.g. network failure on first use) or if tokenization itself fails.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tokenizer")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_count_tokens(model: *const std::ffi::c_char, text: *const std::ffi::c_char) -> usize {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return 0;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return 0;
            }
        };

        if text.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'text'");
            return 0;
        }
        // SAFETY: null check above guarantees text is a valid pointer; string is valid UTF-8 from caller.
        let text_rs = match unsafe { CStr::from_ptr(text) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'text'");
                return 0;
            }
        };

        let result = liter_llm::count_tokens(&model_rs, &text_rs);
        match result {
            Ok(val) => val,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a new LLM client with simple scalar configuration.
///
/// This is the primary binding entry-point. All parameters except `api_key`
/// are optional â omitting them uses the same defaults as
/// `ClientConfigBuilder`.
/// \note Returns `LiterLlmError` if the underlying HTTP client cannot be
/// constructed, or if the resolved provider configuration is invalid.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_client(
    api_key: *const std::ffi::c_char,
    base_url: *const std::ffi::c_char,
    timeout_secs: u64,
    max_retries: u32,
    model_hint: *const std::ffi::c_char,
) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if api_key.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'api_key'");
            return 0;
        }
        // SAFETY: null check above guarantees api_key is a valid pointer; string is valid UTF-8 from caller.
        let api_key_rs = match unsafe { CStr::from_ptr(api_key) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'api_key'");
                return 0;
            }
        };

        let base_url_rs = if base_url.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees base_url is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(base_url) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'base_url'");
                    return 0;
                }
            }
        };

        // Optional numeric primitive: max value of type = None
        let timeout_secs_rs = if timeout_secs == u64::MAX {
            None
        } else {
            Some(timeout_secs)
        };

        // Optional numeric primitive: max value of type = None
        let max_retries_rs = if max_retries == u32::MAX {
            None
        } else {
            Some(max_retries)
        };

        let model_hint_rs = if model_hint.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees model_hint is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(model_hint) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'model_hint'");
                    return 0;
                }
            }
        };

        let result = liter_llm::create_client(api_key_rs, base_url_rs, timeout_secs_rs, max_retries_rs, model_hint_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Create a new LLM client from a JSON string.
///
/// The JSON object accepts the same fields as `liter-llm.toml` (snake_case).
/// \note Returns `LiterLlmError.BadRequest` if `json` is not valid JSON or
/// contains unknown fields.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(feature = "native-http", feature = "wasm-http"))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_create_client_from_json(json: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if json.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'json'");
            return 0;
        }
        // SAFETY: null check above guarantees json is a valid pointer; string is valid UTF-8 from caller.
        let json_rs = match unsafe { CStr::from_ptr(json) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'json'");
                return 0;
            }
        };

        let result = liter_llm::create_client_from_json(&json_rs);
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Decode a base64 data URL into `DecodedDataUrl`.
///
/// Returns `None` for:
/// - Non-data URLs (strings that do not start with `"data:"`).
/// - Malformed prefixes (missing `";base64,"` marker).
/// - Invalid base64 payloads.
///
/// The returned MIME string is extracted verbatim from the URL prefix â
/// it is not validated or normalised.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use liter_llm::image::{encode_data_url, decode_data_url, IMAGE_PNG};
///
/// let url = encode_data_url(b"hello", Some(IMAGE_PNG));
/// let decoded = decode_data_url(&url).expect("valid data URL");
/// assert_eq!(decoded.mime, IMAGE_PNG);
/// assert_eq!(decoded.data, b"hello");
///
/// // Non-data URLs return None.
/// assert!(decode_data_url("https://example.com/img.png").is_none());
///
/// // Missing ;base64, marker returns None.
/// assert!(decode_data_url("data:image/png,plaintext").is_none());
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_decode_data_url(url: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if url.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'url'");
            return 0;
        }
        // SAFETY: null check above guarantees url is a valid pointer; string is valid UTF-8 from caller.
        let url_rs = match unsafe { CStr::from_ptr(url) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'url'");
                return 0;
            }
        };

        let result = liter_llm::image::decode_data_url(&url_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Encode bytes as a base64 data URL: `data:<mime>;base64,<b64>`.
///
/// `mime` defaults to `IMAGE_PNG` when `None`.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
/// \code
/// use liter_llm::image::{encode_data_url, IMAGE_PNG, IMAGE_JPEG};
///
/// let url = encode_data_url(b"\x89PNG", Some(IMAGE_PNG));
/// assert!(url.starts_with("data:image/png;base64,"));
///
/// let url_default = encode_data_url(b"\x89PNG", None);
/// assert!(url_default.starts_with("data:image/png;base64,"));
///
/// let jpeg_url = encode_data_url(b"\xff\xd8\xff", Some(IMAGE_JPEG));
/// assert!(jpeg_url.starts_with("data:image/jpeg;base64,"));
/// \endcode
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_encode_data_url(
    bytes: *const u8,
    bytes_len: usize,
    mime: *const std::ffi::c_char,
) -> *mut std::ffi::c_char {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        set_last_return_len("literllm_encode_data_url", 0);
        if bytes.is_null() && bytes_len > 0 {
            set_last_error(1, "Null pointer passed for parameter 'bytes'");
            return std::ptr::null_mut();
        }
        // SAFETY: when bytes is null, bytes_len is 0 (checked above), so we use an empty slice; otherwise data is valid for len elements.
        let bytes_rs = if bytes.is_null() {
            Vec::new()
        } else {
            unsafe { std::slice::from_raw_parts(bytes, bytes_len) }.to_vec()
        };

        let mime_rs = if mime.is_null() {
            None
        } else {
            // SAFETY: null check above guarantees mime is a valid pointer; string is valid UTF-8 from caller.
            match unsafe { CStr::from_ptr(mime) }.to_str() {
                Ok(s) => Some(s.to_string()),
                Err(_) => {
                    set_last_error(1, "Invalid UTF-8 in parameter 'mime'");
                    return std::ptr::null_mut();
                }
            }
        };

        let result = liter_llm::image::encode_data_url(&bytes_rs, mime_rs.as_deref());
        {
            let __alef_return = result.to_string();
            match CString::new(__alef_return) {
                Ok(cs) => {
                    set_last_return_len("literllm_encode_data_url", cs.as_bytes().len());
                    cs.into_raw()
                }
                Err(_) => {
                    set_last_return_len("literllm_encode_data_url", 0);
                    std::ptr::null_mut()
                }
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            std::ptr::null_mut()
        }
    }
}

/// Return the byte length of the C string most recently returned by `literllm_encode_data_url` on this
/// thread. Returns 0 when the primary call returned null or failed before producing a string. Enables
/// safe slice construction in Zig and Java FFM Panama without a NUL-scan.
/// \note SAFETY: Pointer arguments are ignored and are present only to keep the companion ABI aligned
/// with `literllm_encode_data_url`.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_encode_data_url_len(
    _bytes: *const u8,
    _bytes_len: usize,
    _mime: *const std::ffi::c_char,
) -> usize {
    catch_ffi_panic_preserving_error(0, || last_return_len("literllm_encode_data_url"))
}

/// Install the `ring` crypto provider as the rustls process default, idempotently.
///
/// rustls 0.23+ removed the implicit default provider. This function installs
/// `ring` once per process. Subsequent calls are no-ops. Calling it after
/// another rustls crypto provider has already been installed is safe: the
/// `Err` from `install_default()` is silently ignored.
///
/// Called automatically by every internal `reqwest::Client` constructor
/// (auth providers, default HTTP client). Bindings and downstream consumers
/// reach those constructors transitively, so no manual init is required.
///
/// WASM builds are exempt â the WASM target uses the browser/Node.js fetch
/// API instead of rustls, so no crypto provider is needed.
///
/// Windows builds use native-tls (SChannel) via reqwest, so rustls is not
/// present and no crypto provider installation is needed.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(any(
    all(any(feature = "native-http", feature = "opendal-cache"), not(target_os = "windows")),
    all(any(feature = "native-http", feature = "opendal-cache"), target_os = "windows")
))]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_ensure_crypto_provider() {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(liter_llm::ensure_crypto_provider)) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Install the overlay registry from a raw catalog JSON string, bypassing
/// the network and disk cache entirely.
///
/// Parses and flattens `catalog_json` with the same
/// `registry_from_catalog_str` logic used for the embedded catalog and the
/// network refresh path, then atomically swaps it in as the active overlay.
/// A parse failure returns `CatalogRefreshError.Parse` and leaves any
/// existing overlay untouched.
///
/// This is primarily a testable seam: it lets tests exercise overlay
/// installation and the embedded/overlay fallback behavior in
/// `completion_cost` / `model_info` without a real network
/// call.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_install_catalog_overlay_from_str(catalog_json: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if catalog_json.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'catalog_json'");
            return -1;
        }
        // SAFETY: null check above guarantees catalog_json is a valid pointer; string is valid UTF-8 from caller.
        let catalog_json_rs = match unsafe { CStr::from_ptr(catalog_json) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'catalog_json'");
                return -1;
            }
        };

        let result = liter_llm::cost::refresh::install_catalog_overlay_from_str(&catalog_json_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Look up FFI-friendly pricing and capability metadata for a model.
///
/// Returns `None` if the model is not present in the active pricing
/// registry. Uses the same exact-match-then-prefix-fallback resolution as
/// `model_pricing`; unlike `model_pricing`, the result is an owned
/// `ModelInfo` value safe to hand across the FFI boundary.
///
/// When a runtime catalog refresh has succeeded, this reflects the refreshed
/// (overlay) catalog; otherwise it reflects the embedded catalog. See
/// `model_pricing` for the embedded-only alternative.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_model_info(model: *const std::ffi::c_char) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return 0;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return 0;
            }
        };

        let result = liter_llm::model_info(&model_rs);
        match result {
            Some(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            None => 0,
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Record the estimated USD cost of a completion.
///
/// Call from `CostTrackingService` once a
/// completion's cost has been computed. Emits `gen_ai.client.cost.usd`.
/// If the meter has not been initialized, this call is a no-op.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[cfg(feature = "tower")]
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_record_cost_usd(
    system: *const std::ffi::c_char,
    model: *const std::ffi::c_char,
    operation: *const std::ffi::c_char,
    cost_usd: f64,
) {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if system.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'system'");
            return;
        }
        // SAFETY: null check above guarantees system is a valid pointer; string is valid UTF-8 from caller.
        let system_rs = match unsafe { CStr::from_ptr(system) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'system'");
                return;
            }
        };

        if model.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'model'");
            return;
        }
        // SAFETY: null check above guarantees model is a valid pointer; string is valid UTF-8 from caller.
        let model_rs = match unsafe { CStr::from_ptr(model) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'model'");
                return;
            }
        };

        if operation.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'operation'");
            return;
        }
        // SAFETY: null check above guarantees operation is a valid pointer; string is valid UTF-8 from caller.
        let operation_rs = match unsafe { CStr::from_ptr(operation) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'operation'");
                return;
            }
        };

        let cost_usd_rs = cost_usd;

        liter_llm::tower::metrics::record_cost_usd(&system_rs, &model_rs, &operation_rs, cost_usd_rs)
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            ()
        }
    }
}

/// Refresh the runtime catalog overlay per `config`.
///
/// - `config.enabled == false`: returns `Ok(``RefreshOutcome.Disabled``)`
///   immediately. No network, filesystem, or overlay activity.
/// - A fresh on-disk cache (age < `config.ttl_seconds`) exists at the
///   resolved cache path (`config.cache_path`, or a default under
///   `std::env::temp_dir()`): read + flatten it and install the overlay,
///   returning `Ok(``RefreshOutcome.FromCache``)`. No network request is
///   made.
/// - Otherwise: validate `config.source_url` uses `https`
///   (`CatalogRefreshError.InsecureUrl` otherwise), fetch it, flatten it,
///   install the overlay, best-effort write the raw JSON to the cache path
///   (a cache write failure does not fail the refresh), and return
///   `Ok(``RefreshOutcome.Fetched``)`.
///
/// On any error return, the overlay is left untouched: the previously
/// active registry (a prior successful overlay, or the embedded catalog if
/// none was ever installed) remains in effect. This is what makes the
/// feature air-gap-safe â an unreachable or invalid `source_url` never
/// degrades `completion_cost` / `model_info` below embedded-catalog
/// availability.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_refresh_catalog(config: AlefHandle) -> AlefHandle {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<liter_llm::cost::refresh::CatalogRefreshConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return 0;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return 0;
                }
            }
        }
        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return 0;
        }
        let config_rs_ptr =
            match locked_handle_ptr::<liter_llm::cost::refresh::CatalogRefreshConfig>(&mut __alef_guards, config) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return 0;
                }
            };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr };

        let result = get_ffi_runtime().block_on(async { liter_llm::cost::refresh::refresh_catalog(&config_rs).await });
        match result {
            Ok(val) => match insert_handle(val) {
                Ok(handle) => handle,
                Err(error) => {
                    set_handle_error(&error);
                    0
                }
            },
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Register a custom provider in the global runtime registry.
///
/// The provider will be checked **before** all built-in providers during model
/// detection. If a provider with the same `name` already exists it is replaced.
/// \note Returns an error if the config is invalid (empty name, empty base_url, or
/// no model prefixes).
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_register_custom_provider(config: AlefHandle) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        let mut __alef_requests: Vec<HandleRequest> = [Some(HandleRequest {
            handle: config,
            expected_type: std::any::TypeId::of::<liter_llm::provider::custom::CustomProviderConfig>(),
        })]
        .into_iter()
        .flatten()
        .collect();
        let __alef_values = match acquire_handles(&__alef_requests) {
            Ok(values) => values,
            Err(error) => {
                set_handle_error(&error);
                return -1;
            }
        };
        let mut __alef_guards = Vec::with_capacity(__alef_values.len());
        for (handle, value) in &__alef_values {
            match value.lock() {
                Ok(guard) => __alef_guards.push((*handle, guard)),
                Err(_) => {
                    set_handle_error(&HandleError::RegistryPoisoned);
                    return -1;
                }
            }
        }
        if config == 0 {
            set_last_error(
                ALEF_INVALID_HANDLE_ERROR,
                "zero is not a valid handle for parameter 'config'",
            );
            return -1;
        }
        let config_rs_ptr =
            match locked_handle_ptr::<liter_llm::provider::custom::CustomProviderConfig>(&mut __alef_guards, config) {
                Ok(value) => value,
                Err(error) => {
                    set_handle_error(&error);
                    return -1;
                }
            };
        // SAFETY: the registry entry guard remains held for the duration of this call.
        let config_rs = unsafe { &*config_rs_ptr }.clone();

        let result = liter_llm::provider::custom::register_custom_provider(config_rs);
        match result {
            Ok(()) => 0,
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                -1
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            -1
        }
    }
}

/// Remove a previously registered custom provider by name.
///
/// Returns `true` if a provider with the given name was found and removed,
/// `false` if no such provider existed.
/// \note Returns an error if the custom-provider registry cannot be updated.
/// \note SAFETY: Caller must ensure all pointer arguments are valid or null. Returned pointers must be
/// freed with the appropriate free function.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_unregister_custom_provider(name: *const std::ffi::c_char) -> i32 {
    clear_last_error();
    match std::panic::catch_unwind(std::panic::AssertUnwindSafe(|| {
        if name.is_null() {
            set_last_error(1, "Null pointer passed for parameter 'name'");
            return 0;
        }
        // SAFETY: null check above guarantees name is a valid pointer; string is valid UTF-8 from caller.
        let name_rs = match unsafe { CStr::from_ptr(name) }.to_str() {
            Ok(s) => s.to_string(),
            Err(_) => {
                set_last_error(1, "Invalid UTF-8 in parameter 'name'");
                return 0;
            }
        };

        let result = liter_llm::provider::custom::unregister_custom_provider(&name_rs);
        match result {
            Ok(val) => {
                if val {
                    1
                } else {
                    0
                }
            }
            Err(e) => {
                set_last_error(alef_ffi_error_code(&e), &e.to_string());
                0
            }
        }
    })) {
        Ok(value) => value,
        Err(_) => {
            // A panic can unwind past code that already reported a more specific error via
            // set_last_error/set_handle_error (e.g. a conversion failure right before an
            // unrelated panic deeper in the same call); only stamp the generic panic marker
            // when nothing more specific is already recorded, so that context survives.
            if LAST_ERROR_CODE.with_borrow(|c| *c == 0) {
                let _ = std::panic::catch_unwind(std::panic::AssertUnwindSafe(set_panic_error));
            }
            0
        }
    }
}

/// Return the HTTP status code for the error pointed to by `err`.
/// Returns `0` if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_liter_llm_error_status_code(err: *const liter_llm::error::LiterLlmError) -> u16 {
    if err.is_null() {
        return 0;
    }
    // SAFETY: caller guarantees `err` points to a live `liter_llm::error::LiterLlmError` value
    // allocated by this library, or is null (checked above).
    unsafe { (*err).status_code() }
}

/// Return whether the error pointed to by `err` is transient.
/// Returns `false` if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_liter_llm_error_is_transient(err: *const liter_llm::error::LiterLlmError) -> bool {
    if err.is_null() {
        return false;
    }
    // SAFETY: caller guarantees `err` points to a live `liter_llm::error::LiterLlmError` value
    // allocated by this library, or is null (checked above).
    unsafe { (*err).is_transient() }
}

/// Return the machine-readable error category string for the error pointed
/// to by `err` as a heap-allocated, NUL-terminated C string.
/// The caller must free the returned pointer with `literllm_liter_llm_error_error_type_free`.
/// Returns a null pointer if `err` is null.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_liter_llm_error_error_type(
    err: *const liter_llm::error::LiterLlmError,
) -> *mut std::ffi::c_char {
    if err.is_null() {
        return std::ptr::null_mut();
    }
    // SAFETY: caller guarantees `err` points to a live `liter_llm::error::LiterLlmError` value
    // allocated by this library, or is null (checked above).
    let s = unsafe { (*err).error_type() };
    // SAFETY: `error_type()` returns a `'static str` containing no NUL bytes.
    std::ffi::CString::new(s)
        .map(|c| c.into_raw())
        .unwrap_or(std::ptr::null_mut())
}

/// Free a string previously returned by `literllm_liter_llm_error_error_type`.
/// Passing a null pointer is a no-op.
#[unsafe(no_mangle)]
pub unsafe extern "C" fn literllm_liter_llm_error_error_type_free(ptr: *mut std::ffi::c_char) {
    if !ptr.is_null() {
        // SAFETY: `ptr` was allocated by `CString::into_raw` inside
        // `literllm_liter_llm_error_error_type` and is now being reclaimed by the matching
        // `CString::from_raw`.  Passing null is explicitly allowed.
        unsafe { drop(std::ffi::CString::from_raw(ptr)) };
    }
}
