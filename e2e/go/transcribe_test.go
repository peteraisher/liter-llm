// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8773bb3b130dc49245dd2e8db368223837f627acaa2faa7214d4b8f5b3ead180
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: transcribe
package e2e_test

import (
	"encoding/json"
	"os"
	"strings"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/liter-llm/packages/go"
)

func Test_EdgeTranscribeEmptyAudio(t *testing.T) {
	// Transcription of a silent or empty audio file returns empty text
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"silence.mp3","model":"whisper-1"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_transcribe_empty_audio"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Transcribe(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Text), ``) {
		t.Errorf("expected to contain %s, got %v", ``, result.Text)
	}
}

func Test_EdgeTranscribeWithTimestamps(t *testing.T) {
	// Transcription with verbose JSON response format including timestamp segments
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"audio.mp3","model":"whisper-1","response_format":"verbose_json"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_transcribe_with_timestamps"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Transcribe(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if len(result.Text) == 0 {
		t.Errorf("expected non-empty value")
	}
	if result.Segments != nil {
		assert.Equal(t, len(result.Segments), 3, "expected exactly 3 elements")
	} else {
		t.Errorf("expected exactly 3 elements, got nil")
	}
	if len(result.Segments) == 0 {
		t.Fatalf("expected non-empty %s", `result.Segments`)
	}
	if result.Segments[0].ID != 0 {
		t.Errorf("equals mismatch: got %v", result.Segments[0].ID)
	}
}

func Test_ErrorTranscribeAuth401(t *testing.T) {
	// 401 Unauthorized for transcription with invalid API key
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"audio.mp3","model":"whisper-1"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_transcribe_auth_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Transcribe(req)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	// skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_ErrorTranscribeBadFormat(t *testing.T) {
	// 400 Bad Request when audio format is unsupported
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"audio.xyz","model":"whisper-1"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/error_transcribe_bad_format"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Transcribe(req)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	// skipped: declared error variant 'BadRequest' not substantiated by this backend's generated error type
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_SmokeTranscribeBasic(t *testing.T) {
	// Basic audio transcription
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"audio.mp3","model":"whisper-1"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_transcribe_basic"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Transcribe(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Text), `Hello, this is a test transcription.`) {
		t.Errorf("expected to contain %s, got %v", `Hello, this is a test transcription.`, result.Text)
	}
}

func Test_SmokeTranscribeWithLanguage(t *testing.T) {
	// Audio transcription with explicit language hint
	var req pkg.CreateTranscriptionRequest
	if err := json.Unmarshal([]byte(`{"file":"audio_de.mp3","language":"de","model":"whisper-1"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/smoke_transcribe_with_language"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Transcribe(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	if !strings.Contains(string(result.Text), `Hallo, dies ist ein Testtranskription.`) {
		t.Errorf("expected to contain %s, got %v", `Hallo, dies ist ein Testtranskription.`, result.Text)
	}
}
