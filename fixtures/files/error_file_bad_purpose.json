{
  "id": "error_file_bad_purpose",
  "description": "400 Bad Request when uploading a file with invalid purpose",
  "docs": {
    "topic": "files",
    "stem": "error-file-bad-purpose",
    "title": "400 Bad Request when uploading a file with invalid purpose",
    "coverage_exceptions": {
      "node": {
        "reason": "FilePurpose is a closed enum: the native constructor rejects invalid-purpose before HTTP, so a type-correct snippet cannot demonstrate this HTTP 400 response.",
        "documentation": "crates/liter-llm-node/index.d.ts"
      },
      "wasm": {
        "reason": "WasmFilePurpose contains only declared members; InvalidPurpose does not exist. Accessing that absent JavaScript member yields undefined and coerces to Assistants, so it cannot demonstrate an invalid-purpose HTTP request.",
        "documentation": "crates/liter-llm-wasm/src/lib.rs"
      }
    }
  },
  "category": "files",
  "tags": [
    "error-handling",
    "files"
  ],
  "skip": {
    "languages": [
      "rust",
      "python",
      "kotlin_android",
      "csharp",
      "ruby",
      "node",
      "wasm"
    ],
    "reason": "Typed FilePurpose bindings reject invalid values before HTTP (Rust/Python/Kotlin/C#/Ruby/Node). WASM exposes only declared enum members; an undeclared JavaScript member is undefined and coerces to Assistants, so it cannot send this invalid-purpose request. HTTP 400 is therefore not exercised by these bindings."
  },
  "call": "create_file",
  "input": {
    "file": "data.jsonl",
    "purpose": "invalid-purpose"
  },
  "mock_response": {
    "status": 400,
    "body": {
      "error": {
        "message": "Invalid value for 'purpose': 'invalid-purpose'. Supported values are: 'fine-tune', 'assistants', 'batch'.",
        "type": "invalid_request_error",
        "param": "purpose",
        "code": null
      }
    }
  },
  "assertions": [
    {
      "type": "error",
      "value": "BadRequest"
    },
    {
      "type": "equals",
      "field": "error.status_code",
      "value": 400
    }
  ]
}
