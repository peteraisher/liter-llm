// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:381718f5867767f1fc44d46b644e85c03bf22ea22be88087544d8df032710e20
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: configuration
package e2e_test

import (
	"encoding/json"
	"os"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/liter-llm/packages/go/v2"
)

func Test_CustomBaseUrl(t *testing.T) {
	// Client configured with a custom base URL routes all requests to that endpoint
	var req pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"local-model"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/custom_base_url"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0MessageContent string
	if result.Choices[0].Message.Content != nil {
		choices0MessageContent = result.Choices[0].Message.Content.Text()
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if string(choices0MessageContent) != `Hi there!` {
		t.Errorf("equals mismatch: got %v", choices0MessageContent)
	}
	if string(choices0FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
	if string(result.Model) != `local-model` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_ExtraHeaders(t *testing.T) {
	// Client configured with extra custom headers successfully completes a chat request
	var req pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"gpt-4"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/extra_headers"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if string(choices0FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
}

func Test_LocalProviderLlamacpp(t *testing.T) {
	// llamacpp local provider routes requests via llamacpp/ model prefix with no auth
	var req pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"llamacpp/my-model"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_llamacpp"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0MessageContent string
	if result.Choices[0].Message.Content != nil {
		choices0MessageContent = result.Choices[0].Message.Content.Text()
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if string(choices0MessageContent) != `Hi there! I'm running locally.` {
		t.Errorf("equals mismatch: got %v", choices0MessageContent)
	}
	if string(choices0FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
	if string(result.Model) != `my-model` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_LocalProviderOllama(t *testing.T) {
	// Ollama local provider routes requests via ollama/ model prefix with no auth
	var req pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"ollama/qwen2:0.5b"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_ollama"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0MessageContent string
	if result.Choices[0].Message.Content != nil {
		choices0MessageContent = result.Choices[0].Message.Content.Text()
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if string(choices0MessageContent) != `Hello! How can I help you today?` {
		t.Errorf("equals mismatch: got %v", choices0MessageContent)
	}
	if string(choices0FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
	if string(result.Model) != `qwen2:0.5b` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}

func Test_LocalProviderVllm(t *testing.T) {
	// vLLM local provider routes requests via vllm/ model prefix with no auth
	var req pkg.ChatCompletionRequest
	if err := json.Unmarshal([]byte(`{"messages":[{"content":"Hello","role":"user"}],"model":"vllm/meta-llama/Llama-3.2-1B"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/local_provider_vllm"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Chat(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	var choices0MessageContent string
	if result.Choices[0].Message.Content != nil {
		choices0MessageContent = result.Choices[0].Message.Content.Text()
	}
	var choices0FinishReason string
	if result.Choices[0].FinishReason != nil {
		choices0FinishReason = string(*result.Choices[0].FinishReason)
	}
	assert.Equal(t, len(result.Choices), 1, "expected exactly 1 elements")
	if string(choices0MessageContent) != `Hello! How may I assist you?` {
		t.Errorf("equals mismatch: got %v", choices0MessageContent)
	}
	if string(choices0FinishReason) != `stop` {
		t.Errorf("equals mismatch: got %v", choices0FinishReason)
	}
	if string(result.Model) != `meta-llama/Llama-3.2-1B` {
		t.Errorf("equals mismatch: got %v", result.Model)
	}
}
