// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:8fd7e58063dd104218cc7286a9d44843718b726d8eefa0c6feddcdba6f002f4b
// To regenerate: alef generate
// To verify freshness: alef verify

// E2e tests for category: embed
package e2e_test

import (
	"encoding/json"
	"os"
	"testing"

	"github.com/stretchr/testify/assert"

	pkg "github.com/xberg-io/liter-llm/packages/go/v2"
)

func Test_BatchEmbed(t *testing.T) {
	// Embedding request with multiple input strings returns one embedding object per input
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":["Hello","World"],"model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/batch_embed"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 2, "expected exactly 2 elements")
	if len(result.Data) == 0 {
		t.Fatalf("expected non-empty %s", `result.Data`)
	}
	assert.Equal(t, len(result.Data[0].Embedding), 5, "expected exactly 5 elements")
}

func Test_EdgeEmbedBatchInput(t *testing.T) {
	// Embedding request with multiple inputs returns multiple embedding objects
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":["Hello world","Goodbye world"],"model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_embed_batch_input"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 2, "expected exactly 2 elements")
	if result.Data[0].Index != 0 {
		t.Errorf("equals mismatch: got %v", result.Data[0].Index)
	}
	if result.Data[1].Index != 1 {
		t.Errorf("equals mismatch: got %v", result.Data[1].Index)
	}
}

func Test_EdgeEmbedEmptyInput(t *testing.T) {
	// Embedding request with empty string input returns empty data array
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":"","model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/edge_embed_empty_input"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 0, "expected exactly 0 elements")
}

func Test_EmbedBase64(t *testing.T) {
	// Embedding request with encoding_format base64 decodes to a float array embedding
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"encoding_format":"base64","input":"Test input","model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/embed_base64"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
	assert.Equal(t, len(result.Data[0].Embedding), 4, "expected exactly 4 elements")
}

func Test_EmbedEncodingFormat(t *testing.T) {
	// Embedding request with explicit encoding_format of float returns float array embeddings
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"encoding_format":"float","input":"Test input","model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/embed_encoding_format"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
	assert.Equal(t, len(result.Data[0].Embedding), 5, "expected exactly 5 elements")
}

func Test_EmbedError401(t *testing.T) {
	// 401 Unauthorized error on embedding request when API key is invalid
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":"Hello world","model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/embed_error_401"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	_, err := client.Embed(req)
	if err == nil {
		t.Errorf("expected an error, but call succeeded")
	}
	// skipped: declared error variant 'Authentication' not substantiated by this backend's generated error type
	// skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
}

func Test_EmbedWithDimensions(t *testing.T) {
	// Embedding request with explicit dimensions parameter returns embeddings of the requested size
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"dimensions":256,"input":"Hello world","model":"text-embedding-3-small"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/embed_with_dimensions"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
	assert.Equal(t, len(result.Data[0].Embedding), 8, "expected exactly 8 elements")
}

func Test_LocalEmbedOllama(t *testing.T) {
	// Embedding request via Ollama local provider with all-minilm model
	var req pkg.EmbeddingRequest
	if err := json.Unmarshal([]byte(`{"input":"The quick brown fox jumps over the lazy dog","model":"ollama/all-minilm"}`), &req); err != nil {
		t.Fatalf("config parse failed: %v", err)
	}
	mockURL := os.Getenv("MOCK_SERVER_URL") + "/fixtures/local_embed_ollama"
	client, clientErr := pkg.CreateClient("test-key", &mockURL, nil, nil, nil)
	if clientErr != nil {
		t.Fatalf("create client failed: %v", clientErr)
	}
	result, err := client.Embed(req)
	if err != nil {
		t.Fatalf("call failed: %v", err)
	}
	assert.Equal(t, len(result.Data), 1, "expected exactly 1 elements")
	assert.Equal(t, len(result.Data[0].Embedding), 32, "expected exactly 32 elements")
}
