// This file is auto-generated by alef — DO NOT EDIT.
// alef:hash:b3055071b0f3781d3bef35e8906941fd3bc052d888f9acf7df2c745065c046c3
// To regenerate: alef generate
// To verify freshness: alef verify
package io.xberg.literllm.android.e2e

import org.junit.jupiter.api.Test
import kotlin.test.assertEquals
import kotlin.test.assertTrue
import kotlin.test.assertFalse
import kotlin.test.assertFailsWith
import kotlin.test.assertNotNull
import kotlinx.coroutines.runBlocking
import io.xberg.literllm.android.LiterLlm
import com.fasterxml.jackson.databind.ObjectMapper
import com.fasterxml.jackson.datatype.jdk8.Jdk8Module
import com.fasterxml.jackson.module.kotlin.registerKotlinModule
import kotlin.time.Duration.Companion.milliseconds
import io.xberg.literllm.android.CreateBatchRequest

/** E2e tests for category: batches. */
class BatchesTest {

    companion object {
        init {
            try {
                System.loadLibrary("literllm_jni")
            } catch (e: UnsatisfiedLinkError) {
                System.err.println("Failed to load literllm_jni library: ${e.message}")
                val libPath = System.getProperty("java.library.path")
                System.err.println("java.library.path: $libPath")
                throw e
            }
        }
        private val MAPPER = ObjectMapper().registerModule(Jdk8Module()).registerModule(
            com.fasterxml.jackson.databind.module.SimpleModule()
                .addSerializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonSerializer<kotlin.time.Duration>() {
                    override fun serialize(value: kotlin.time.Duration, gen: com.fasterxml.jackson.core.JsonGenerator, serializers: com.fasterxml.jackson.databind.SerializerProvider) {
                        gen.writeNumber(value.inWholeMilliseconds)
                    }
                })
                .addDeserializer(kotlin.time.Duration::class.java, object : com.fasterxml.jackson.databind.JsonDeserializer<kotlin.time.Duration>() {
                    override fun deserialize(p: com.fasterxml.jackson.core.JsonParser, ctxt: com.fasterxml.jackson.databind.DeserializationContext): kotlin.time.Duration {
                        return p.longValue.milliseconds
                    }
                })
        ).registerKotlinModule().setPropertyNamingStrategy(com.fasterxml.jackson.databind.PropertyNamingStrategies.SNAKE_CASE)
    }
    @Test
    fun testEdgeBatchAlreadyCancelled(): Unit = runBlocking {
        // Attempt to cancel an already-cancelled batch
        assertFailsWith<Exception> {
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.edge_batch_already_cancelled", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/edge_batch_already_cancelled"))
            client.cancelBatch("batch-cancelled001")
            client.close()
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
        Unit
    }

    @Test
    fun testEdgeBatchEmptyList(): Unit = runBlocking {
        // List batches when no batches exist
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.edge_batch_empty_list", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/edge_batch_empty_list"))
        val result = client.listBatches()
        // not_error: covered by the bare Optional's own assertion
        assertEquals(0, result.data.size, "expected exactly 0 elements")
        client.close()
    }

    @Test
    fun testErrorBatchAuth401(): Unit = runBlocking {
        // 401 Unauthorized when creating a batch with invalid API key
        assertFailsWith<Exception> {
            val req = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.error_batch_auth_401", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/error_batch_auth_401"))
            client.createBatch(req)
            client.close()
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
        Unit
    }

    @Test
    fun testErrorBatchInvalidFile(): Unit = runBlocking {
        // 400 Bad Request when creating a batch with invalid input file
        assertFailsWith<Exception> {
            val req = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-wrong-purpose\"}", CreateBatchRequest::class.java)
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.error_batch_invalid_file", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/error_batch_invalid_file"))
            client.createBatch(req)
            client.close()
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
        Unit
    }

    @Test
    fun testErrorBatchNotFound(): Unit = runBlocking {
        // 404 Not Found when retrieving a nonexistent batch
        assertFailsWith<Exception> {
            val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.error_batch_not_found", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/error_batch_not_found"))
            client.retrieveBatch("batch-nonexistent")
            client.close()
        }
        // skipped: assertion type 'equals' has no accessor for error field error.status_code in this backend
        Unit
    }

    @Test
    fun testSmokeBatchCompleted(): Unit = runBlocking {
        // Retrieve a completed batch job with output file
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.smoke_batch_completed", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/smoke_batch_completed"))
        val result = client.retrieveBatch("batch-ghi789")
        // not_error: covered by the bare Optional's own assertion
        assertFalse(result.id.isEmpty(), "expected non-empty value")
        assertEquals("completed", result.status.toWire())
        client.close()
    }

    @Test
    fun testSmokeCancelBatch(): Unit = runBlocking {
        // Cancel a running batch job
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.smoke_cancel_batch", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/smoke_cancel_batch"))
        val result = client.cancelBatch("batch-def456")
        // not_error: covered by the bare Optional's own assertion
        assertFalse(result.id.isEmpty(), "expected non-empty value")
        assertEquals("cancelling", result.status.toWire())
        client.close()
    }

    @Test
    fun testSmokeCreateBatch(): Unit = runBlocking {
        // Create a new batch processing job
        val req = MAPPER.readValue("{\"completion_window\":\"24h\",\"endpoint\":\"/v1/chat/completions\",\"input_file_id\":\"file-abc123\"}", CreateBatchRequest::class.java)
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.smoke_create_batch", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/smoke_create_batch"))
        val result = client.createBatch(req)
        // not_error: covered by the bare Optional's own assertion
        assertFalse(result.id.isEmpty(), "expected non-empty value")
        assertEquals("validating", result.status.toWire())
        client.close()
    }

    @Test
    fun testSmokeListBatches(): Unit = runBlocking {
        // List all batch jobs
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.smoke_list_batches", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/smoke_list_batches"))
        val result = client.listBatches()
        // not_error: covered by the bare Optional's own assertion
        assertEquals(2, result.data.size, "expected exactly 2 elements")
        client.close()
    }

    @Test
    fun testSmokeRetrieveBatch(): Unit = runBlocking {
        // Retrieve the status of a batch job
        val client = LiterLlm.createClient(apiKey = "test-key", baseUrl = System.getProperty("mockServer.smoke_retrieve_batch", (System.getProperty("mockServerUrl", System.getenv("MOCK_SERVER_URL") ?: "") ?: "") + "/fixtures/smoke_retrieve_batch"))
        val result = client.retrieveBatch("batch-abc123")
        // not_error: covered by the bare Optional's own assertion
        assertFalse(result.id.isEmpty(), "expected non-empty value")
        assertEquals("in_progress", result.status.toWire())
        client.close()
    }

}
