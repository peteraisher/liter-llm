---
id: fixture_wasm_azure_stream
language: typescript
target: wasm
requires: []
side_effect: safe
---

<!-- This file is auto-generated by alef — DO NOT EDIT. -->
<!-- alef:hash:6b2a044f7dfe1ef07433f1cf9ff23f7c970a2251af4599a6bc3598b119e016ac -->
<!-- To regenerate: alef e2e generate -->
<!-- To verify freshness: alef verify -->

Streaming chat completion via Azure OpenAI — verifies the azure/ prefix routes correctly and SSE chunks are delivered in the standard OpenAI chat.completion.chunk shape

```typescript title="WebAssembly"
import { WasmChatCompletionChunk, WasmChatCompletionRequest, createClient } from "@xberg-io/liter-llm-wasm";
async function main() {
  const req: WasmChatCompletionRequest = (() => { const _u0 = WasmChatCompletionRequest.default(); _u0.messages = [{ content: "Count to 3", role: "user" }]; _u0.model = "azure/gpt-4"; _u0.stream = true; _u0.temperature = 0; return _u0; })();
  const client = createClient("your-api-key");
  try {
    const result = await client.chatStream(req);
    try {
      while (true) {
        const resultChunk: WasmChatCompletionChunk | null = await result.next();
        if (resultChunk === null) break;
        try {
          console.log(resultChunk);
        } finally {
          resultChunk.free();
        }
      }
    } finally {
      result.free();
    }
  } finally {
    client.free();
  }
}

void main();
```
