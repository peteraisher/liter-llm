# This file is auto-generated by alef — DO NOT EDIT.
# alef:hash:b469fefd3b23c57f6a37cfb6f6255ef6a09be2af4a126cc88908dd62a4c2dfe2
# To regenerate: alef generate
# To verify freshness: alef verify
"""Public API for conversion."""

import json
from typing import Any, AsyncIterator, TypeVar, TypedDict, cast, overload

import liter_llm._internal_bindings as _rust

from ._internal_bindings import CustomProviderConfig, DecodedDataUrl, DefaultClient, EmbeddingResponse, ModelInfo, ModerationResponse, OcrResponse, ProviderCapabilities, ProviderConfig, RefreshOutcome, RerankResponse, SearchResponse
from .options import BatchListQuery, BatchListResponse, BatchObject, CatalogRefreshConfig, ChatCompletionChunk, ChatCompletionRequest, ChatCompletionResponse, CreateBatchRequest, CreateFileRequest, CreateImageRequest, CreateResponseRequest, CreateSpeechRequest, CreateTranscriptionRequest, DeleteResponse, EmbeddingRequest, FileListQuery, FileListResponse, FileObject, ImagesResponse, ModelsListResponse, ModerationRequest, OcrRequest, RerankRequest, ResponseObject, ResponseTool, SearchRequest, StreamOptions, TranscriptionResponse, _from_native_batch_list_response, _from_native_batch_object, _from_native_chat_completion_chunk, _from_native_chat_completion_response, _from_native_delete_response, _from_native_file_list_response, _from_native_file_object, _from_native_images_response, _from_native_models_list_response, _from_native_response_object, _from_native_transcription_response

_E = TypeVar("_E")

def _pascal_to_snake(value: str) -> str:
    """Convert PascalCase/camelCase to snake_case (AtxClosed -> atx_closed)."""
    out_chars: list[str] = []
    for index, ch in enumerate(value):
        if ch.isupper() and index > 0 and (value[index - 1].islower() or (index + 1 < len(value) and value[index + 1].islower())):
            out_chars.append("_")
        out_chars.append(ch.lower())
    return "".join(out_chars)


def _coerce_enum(enum_cls: type[_E], value: object) -> _E:
    """Coerce a string/alias value into the matching pyclass enum instance."""
    if isinstance(value, enum_cls):
        return value
    if value is None:
        msg = f"unknown {getattr(enum_cls, '__name__', enum_cls)!s} value: {value!r}"
        raise ValueError(msg)
    s = str(value).replace("-", "_").replace(" ", "_")
    snake = _pascal_to_snake(s)
    candidates = (
        s,
        s.upper(),
        s.lower(),
        snake,
        snake.upper(),
        "".join(part.capitalize() for part in s.split("_")),
        "".join(part.capitalize() for part in snake.split("_")),
    )
    for candidate in candidates:
        attr = getattr(enum_cls, candidate, None)
        if isinstance(attr, enum_cls):
            return attr
    msg = f"unknown {getattr(enum_cls, '__name__', enum_cls)!s} value: {value!r}"
    raise ValueError(msg)


@overload
def _to_rust_stream_options(value: None) -> None: ...


@overload
def _to_rust_stream_options(value: StreamOptions | dict[str, Any] | str) -> _rust.StreamOptions: ...
def _to_rust_stream_options(value: StreamOptions | dict[str, Any] | str | None) -> _rust.StreamOptions | None:
    """Convert Python StreamOptions to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = StreamOptions(**value)
    if value is None:
        return None
    value = cast(StreamOptions, value)
    return _rust.StreamOptions(
        include_usage=value.include_usage,
    )


@overload
def _to_rust_chat_completion_request(value: None) -> None: ...


@overload
def _to_rust_chat_completion_request(value: ChatCompletionRequest | dict[str, Any] | str) -> _rust.ChatCompletionRequest: ...
def _to_rust_chat_completion_request(
    value: ChatCompletionRequest | dict[str, Any] | str | None,
) -> _rust.ChatCompletionRequest | None:
    """Convert Python ChatCompletionRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        if "reasoning_effort" in value and value["reasoning_effort"] is not None:
            value["reasoning_effort"] = _coerce_enum(_rust.ReasoningEffort, value["reasoning_effort"])
        if "stream_options" in value and value["stream_options"] is not None:
            value["stream_options"] = _to_rust_stream_options(value["stream_options"])
        if "stop" in value and value["stop"] is not None and not isinstance(value["stop"], _rust.StopSequence):
            value["stop"] = _rust.StopSequence(value["stop"])
        if "tool_choice" in value and value["tool_choice"] is not None and not isinstance(value["tool_choice"], _rust.ToolChoice):
            value["tool_choice"] = _rust.ToolChoice(value["tool_choice"])
        if "response_format" in value and value["response_format"] is not None and not isinstance(value["response_format"], _rust.ResponseFormat):
            value["response_format"] = _rust.ResponseFormat(value["response_format"])
        value = ChatCompletionRequest(**value)
    if value is None:
        return None
    value = cast(ChatCompletionRequest, value)
    return _rust.ChatCompletionRequest(
        model=value.model,
        messages=[v if isinstance(v, _rust.Message) else _rust.Message(v) for v in value.messages],
        temperature=value.temperature,
        top_p=value.top_p,
        n=value.n,
        stream=value.stream,
        stop=(value.stop if isinstance(value.stop, _rust.StopSequence) else _rust.StopSequence(value.stop)) if value.stop is not None else None,
        max_tokens=value.max_tokens,
        presence_penalty=value.presence_penalty,
        frequency_penalty=value.frequency_penalty,
        logit_bias=value.logit_bias,
        user=value.user,
        tools=value.tools,
        tool_choice=(value.tool_choice if isinstance(value.tool_choice, _rust.ToolChoice) else _rust.ToolChoice(value.tool_choice)) if value.tool_choice is not None else None,
        parallel_tool_calls=value.parallel_tool_calls,
        response_format=(value.response_format if isinstance(value.response_format, _rust.ResponseFormat) else _rust.ResponseFormat(value.response_format)) if value.response_format is not None else None,
        stream_options=_to_rust_stream_options(value.stream_options),
        seed=value.seed,
        reasoning_effort=_coerce_enum(_rust.ReasoningEffort, value.reasoning_effort) if value.reasoning_effort is not None else None,
        modalities=([_coerce_enum(_rust.Modality, v) for v in value.modalities] if value.modalities is not None else None),
        logprobs=value.logprobs,
        top_logprobs=value.top_logprobs,
        max_completion_tokens=value.max_completion_tokens,
        service_tier=value.service_tier,
        store=value.store,
        metadata=value.metadata,
        prediction=(json.dumps(value.prediction) if isinstance(value.prediction, (dict, list)) else value.prediction),
        audio=(json.dumps(value.audio) if isinstance(value.audio, (dict, list)) else value.audio),
        web_search_options=(json.dumps(value.web_search_options) if isinstance(value.web_search_options, (dict, list)) else value.web_search_options),
        extra_body=(json.dumps(value.extra_body) if isinstance(value.extra_body, (dict, list)) else value.extra_body),
    )


@overload
def _to_rust_catalog_refresh_config(value: None) -> None: ...


@overload
def _to_rust_catalog_refresh_config(value: CatalogRefreshConfig | dict[str, Any] | str) -> _rust.CatalogRefreshConfig: ...
def _to_rust_catalog_refresh_config(
    value: CatalogRefreshConfig | dict[str, Any] | str | None,
) -> _rust.CatalogRefreshConfig | None:
    """Convert Python CatalogRefreshConfig to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CatalogRefreshConfig(**value)
    if value is None:
        return None
    value = cast(CatalogRefreshConfig, value)
    return _rust.CatalogRefreshConfig(
        enabled=value.enabled,
        source_url=value.source_url,
        ttl_seconds=value.ttl_seconds,
        cache_path=value.cache_path,
    )


@overload
def _to_rust_embedding_request(value: None) -> None: ...


@overload
def _to_rust_embedding_request(value: EmbeddingRequest | dict[str, Any] | str) -> _rust.EmbeddingRequest: ...
def _to_rust_embedding_request(value: EmbeddingRequest | dict[str, Any] | str | None) -> _rust.EmbeddingRequest | None:
    """Convert Python EmbeddingRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        if "encoding_format" in value and value["encoding_format"] is not None:
            value["encoding_format"] = _coerce_enum(_rust.EmbeddingFormat, value["encoding_format"])
        if "input" in value and value["input"] is not None and not isinstance(value["input"], _rust.EmbeddingInput):
            value["input"] = _rust.EmbeddingInput(value["input"])
        value = EmbeddingRequest(**value)
    if value is None:
        return None
    value = cast(EmbeddingRequest, value)
    return _rust.EmbeddingRequest(
        model=value.model,
        input=value.input if isinstance(value.input, _rust.EmbeddingInput) else _rust.EmbeddingInput(value.input),
        encoding_format=_coerce_enum(_rust.EmbeddingFormat, value.encoding_format) if value.encoding_format is not None else None,
        dimensions=value.dimensions,
        user=value.user,
    )


@overload
def _to_rust_create_image_request(value: None) -> None: ...


@overload
def _to_rust_create_image_request(value: CreateImageRequest | dict[str, Any] | str) -> _rust.CreateImageRequest: ...
def _to_rust_create_image_request(
    value: CreateImageRequest | dict[str, Any] | str | None,
) -> _rust.CreateImageRequest | None:
    """Convert Python CreateImageRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CreateImageRequest(**value)
    if value is None:
        return None
    value = cast(CreateImageRequest, value)
    return _rust.CreateImageRequest(
        prompt=value.prompt,
        model=value.model,
        n=value.n,
        size=value.size,
        quality=value.quality,
        style=value.style,
        response_format=value.response_format,
        user=value.user,
    )


@overload
def _to_rust_create_transcription_request(value: None) -> None: ...


@overload
def _to_rust_create_transcription_request(value: CreateTranscriptionRequest | dict[str, Any] | str) -> _rust.CreateTranscriptionRequest: ...
def _to_rust_create_transcription_request(
    value: CreateTranscriptionRequest | dict[str, Any] | str | None,
) -> _rust.CreateTranscriptionRequest | None:
    """Convert Python CreateTranscriptionRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CreateTranscriptionRequest(**value)
    if value is None:
        return None
    value = cast(CreateTranscriptionRequest, value)
    return _rust.CreateTranscriptionRequest(
        model=value.model,
        file=value.file,
        language=value.language,
        prompt=value.prompt,
        response_format=value.response_format,
        temperature=value.temperature,
    )


@overload
def _to_rust_moderation_request(value: None) -> None: ...


@overload
def _to_rust_moderation_request(value: ModerationRequest | dict[str, Any] | str) -> _rust.ModerationRequest: ...
def _to_rust_moderation_request(value: ModerationRequest | dict[str, Any] | str | None) -> _rust.ModerationRequest | None:
    """Convert Python ModerationRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        if "input" in value and value["input"] is not None and not isinstance(value["input"], _rust.ModerationInput):
            value["input"] = _rust.ModerationInput(value["input"])
        value = ModerationRequest(**value)
    if value is None:
        return None
    value = cast(ModerationRequest, value)
    return _rust.ModerationRequest(
        input=value.input if isinstance(value.input, _rust.ModerationInput) else _rust.ModerationInput(value.input),
        model=value.model,
    )


@overload
def _to_rust_rerank_request(value: None) -> None: ...


@overload
def _to_rust_rerank_request(value: RerankRequest | dict[str, Any] | str) -> _rust.RerankRequest: ...
def _to_rust_rerank_request(value: RerankRequest | dict[str, Any] | str | None) -> _rust.RerankRequest | None:
    """Convert Python RerankRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = RerankRequest(**value)
    if value is None:
        return None
    value = cast(RerankRequest, value)
    return _rust.RerankRequest(
        model=value.model,
        query=value.query,
        documents=[v if isinstance(v, _rust.RerankDocument) else _rust.RerankDocument(v) for v in value.documents],
        top_n=value.top_n,
        return_documents=value.return_documents,
    )


@overload
def _to_rust_search_request(value: None) -> None: ...


@overload
def _to_rust_search_request(value: SearchRequest | dict[str, Any] | str) -> _rust.SearchRequest: ...
def _to_rust_search_request(value: SearchRequest | dict[str, Any] | str | None) -> _rust.SearchRequest | None:
    """Convert Python SearchRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = SearchRequest(**value)
    if value is None:
        return None
    value = cast(SearchRequest, value)
    return _rust.SearchRequest(
        model=value.model,
        query=value.query,
        max_results=value.max_results,
        search_domain_filter=value.search_domain_filter,
        country=value.country,
    )


@overload
def _to_rust_create_speech_request(value: None) -> None: ...


@overload
def _to_rust_create_speech_request(value: CreateSpeechRequest | dict[str, Any] | str) -> _rust.CreateSpeechRequest: ...
def _to_rust_create_speech_request(
    value: CreateSpeechRequest | dict[str, Any] | str | None,
) -> _rust.CreateSpeechRequest | None:
    """Convert Python CreateSpeechRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CreateSpeechRequest(**value)
    if value is None:
        return None
    value = cast(CreateSpeechRequest, value)
    return _rust.CreateSpeechRequest(
        model=value.model,
        input=value.input,
        voice=value.voice,
        response_format=value.response_format,
        speed=value.speed,
    )


@overload
def _to_rust_ocr_request(value: None) -> None: ...


@overload
def _to_rust_ocr_request(value: OcrRequest | dict[str, Any] | str) -> _rust.OcrRequest: ...
def _to_rust_ocr_request(value: OcrRequest | dict[str, Any] | str | None) -> _rust.OcrRequest | None:
    """Convert Python OcrRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        if "document" in value and value["document"] is not None and not isinstance(value["document"], _rust.OcrDocument):
            value["document"] = _rust.OcrDocument(value["document"])
        value = OcrRequest(**value)
    if value is None:
        return None
    value = cast(OcrRequest, value)
    return _rust.OcrRequest(
        model=value.model,
        document=value.document if isinstance(value.document, _rust.OcrDocument) else _rust.OcrDocument(value.document),
        pages=value.pages,
        include_image_base64=value.include_image_base64,
    )


@overload
def _to_rust_create_file_request(value: None) -> None: ...


@overload
def _to_rust_create_file_request(value: CreateFileRequest | dict[str, Any] | str) -> _rust.CreateFileRequest: ...
def _to_rust_create_file_request(value: CreateFileRequest | dict[str, Any] | str | None) -> _rust.CreateFileRequest | None:
    """Convert Python CreateFileRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        if "purpose" in value and value["purpose"] is not None:
            value["purpose"] = _coerce_enum(_rust.FilePurpose, value["purpose"])
        value = CreateFileRequest(**value)
    if value is None:
        return None
    value = cast(CreateFileRequest, value)
    return _rust.CreateFileRequest(
        file=value.file,
        purpose=_coerce_enum(_rust.FilePurpose, value.purpose),
        filename=value.filename,
    )


@overload
def _to_rust_file_list_query(value: None) -> None: ...


@overload
def _to_rust_file_list_query(value: FileListQuery | dict[str, Any] | str) -> _rust.FileListQuery: ...
def _to_rust_file_list_query(value: FileListQuery | dict[str, Any] | str | None) -> _rust.FileListQuery | None:
    """Convert Python FileListQuery to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = FileListQuery(**value)
    if value is None:
        return None
    value = cast(FileListQuery, value)
    return _rust.FileListQuery(
        purpose=value.purpose,
        limit=value.limit,
        after=value.after,
    )


@overload
def _to_rust_create_batch_request(value: None) -> None: ...


@overload
def _to_rust_create_batch_request(value: CreateBatchRequest | dict[str, Any] | str) -> _rust.CreateBatchRequest: ...
def _to_rust_create_batch_request(
    value: CreateBatchRequest | dict[str, Any] | str | None,
) -> _rust.CreateBatchRequest | None:
    """Convert Python CreateBatchRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CreateBatchRequest(**value)
    if value is None:
        return None
    value = cast(CreateBatchRequest, value)
    return _rust.CreateBatchRequest(
        input_file_id=value.input_file_id,
        endpoint=value.endpoint,
        completion_window=value.completion_window,
        metadata=(json.dumps(value.metadata) if isinstance(value.metadata, (dict, list)) else value.metadata),
    )


@overload
def _to_rust_batch_list_query(value: None) -> None: ...


@overload
def _to_rust_batch_list_query(value: BatchListQuery | dict[str, Any] | str) -> _rust.BatchListQuery: ...
def _to_rust_batch_list_query(value: BatchListQuery | dict[str, Any] | str | None) -> _rust.BatchListQuery | None:
    """Convert Python BatchListQuery to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = BatchListQuery(**value)
    if value is None:
        return None
    value = cast(BatchListQuery, value)
    return _rust.BatchListQuery(
        limit=value.limit,
        after=value.after,
    )


@overload
def _to_rust_response_tool(value: None) -> None: ...


@overload
def _to_rust_response_tool(value: ResponseTool | dict[str, Any] | str) -> _rust.ResponseTool: ...
def _to_rust_response_tool(value: ResponseTool | dict[str, Any] | str | None) -> _rust.ResponseTool | None:
    """Convert Python ResponseTool to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        # Alias serde-renamed keys back to Rust field names
        if "type" in value and "tool_type" not in value:
            value["tool_type"] = value.pop("type")
        value = ResponseTool(**value)
    if value is None:
        return None
    value = cast(ResponseTool, value)
    return _rust.ResponseTool(
        type=value.tool_type,
        config=(json.dumps(value.config) if isinstance(value.config, (dict, list)) else value.config),
    )


@overload
def _to_rust_create_response_request(value: None) -> None: ...


@overload
def _to_rust_create_response_request(value: CreateResponseRequest | dict[str, Any] | str) -> _rust.CreateResponseRequest: ...
def _to_rust_create_response_request(
    value: CreateResponseRequest | dict[str, Any] | str | None,
) -> _rust.CreateResponseRequest | None:
    """Convert Python CreateResponseRequest to Rust binding type."""
    if isinstance(value, str):
        value = json.loads(value)
    if isinstance(value, dict):
        value = CreateResponseRequest(**value)
    if value is None:
        return None
    value = cast(CreateResponseRequest, value)
    return _rust.CreateResponseRequest(
        model=value.model,
        input=(json.dumps(value.input) if isinstance(value.input, (dict, list)) else value.input),
        instructions=value.instructions,
        tools=([_to_rust_response_tool(v) for v in value.tools] if value.tools is not None else None),
        temperature=value.temperature,
        max_output_tokens=value.max_output_tokens,
        metadata=(json.dumps(value.metadata) if isinstance(value.metadata, (dict, list)) else value.metadata),
        extra_body=(json.dumps(value.extra_body) if isinstance(value.extra_body, (dict, list)) else value.extra_body),
        stream=value.stream,
    )


def all_providers() -> list[ProviderConfig]:
    """Return all provider configs from the registry."""
    return _rust.all_providers()


def capabilities(provider_name: str) -> _rust.ProviderCapabilities:
    """Return the capability flags for a named provider."""
    return _rust.capabilities(provider_name=provider_name)


def check_bound(context: str, current_len: int, incoming: int, limit: int) -> None:
    """Assert that `current_len + incoming` does not exceed `limit`."""
    _rust.check_bound(context=context, current_len=current_len, incoming=incoming, limit=limit)


def clear() -> None:
    """Remove all guardrails from the global registry."""
    _rust.clear()


def clear_catalog_overlay() -> None:
    """Clear the overlay registry, reverting `completion_cost`, `completion_cost_with_cache`, an."""
    _rust.clear_catalog_overlay()


def completion_cost(model: str, prompt_tokens: int, completion_tokens: int) -> float | None:
    """Calculate the estimated cost of a completion given a model name and token counts."""
    return _rust.completion_cost(model=model, prompt_tokens=prompt_tokens, completion_tokens=completion_tokens)


def completion_cost_with_cache(
    model: str,
    prompt_tokens: int,
    cached_tokens: int,
    completion_tokens: int,
) -> float | None:
    """Calculate the estimated cost of a completion, accounting for cached (cache-hit) prompt to."""
    return _rust.completion_cost_with_cache(model=model, prompt_tokens=prompt_tokens, cached_tokens=cached_tokens, completion_tokens=completion_tokens)


def complex_provider_names() -> list[str]:
    """Return the set of complex provider names."""
    return _rust.complex_provider_names()


def count_request_tokens(model: str, req: ChatCompletionRequest | None = None) -> int:
    """Count tokens for a full [`ChatCompletionRequest`]."""
    _rust_req = _to_rust_chat_completion_request(req) if req is not None else _rust.ChatCompletionRequest()
    return _rust.count_request_tokens(model=model, req=_rust_req)


def count_tokens(model: str, text: str) -> int:
    """Count tokens in a text string using the tokenizer for the given model."""
    return _rust.count_tokens(model=model, text=text)


def create_client(
    api_key: str,
    base_url: str | None = None,
    timeout_secs: int | None = None,
    max_retries: int | None = None,
    model_hint: str | None = None,
) -> _rust.DefaultClient:
    """Create a new LLM client with simple scalar configuration."""
    return _rust.create_client(api_key=api_key, base_url=base_url, timeout_secs=timeout_secs, max_retries=max_retries, model_hint=model_hint)


def create_client_from_json(json: str) -> _rust.DefaultClient:
    """Create a new LLM client from a JSON string."""
    return _rust.create_client_from_json(json=json)


def decode_data_url(url: str) -> _rust.DecodedDataUrl | None:
    """Decode a base64 data URL into [`DecodedDataUrl`]."""
    return _rust.decode_data_url(url=url)


def encode_data_url(
    bytes: bytes,  # noqa: A002
    mime: str | None = None,
) -> str:
    """Encode bytes as a base64 data URL: `data:<mime>;base64,<b64>`."""
    return _rust.encode_data_url(bytes=bytes, mime=mime)


def ensure_crypto_provider() -> None:
    """Install the `ring` crypto provider as the rustls process default, idempotently."""
    _rust.ensure_crypto_provider()


def install_catalog_overlay_from_str(catalog_json: str) -> None:
    """Install the overlay registry from a raw catalog JSON string, bypassing the network and di."""
    _rust.install_catalog_overlay_from_str(catalog_json=catalog_json)


def model_info(model: str) -> _rust.ModelInfo | None:
    """Look up FFI-friendly pricing and capability metadata for a model."""
    return _rust.model_info(model=model)


def record_cost_usd(system: str, model: str, operation: str, cost_usd: float) -> None:
    """Record the estimated USD cost of a completion."""
    _rust.record_cost_usd(system=system, model=model, operation=operation, cost_usd=cost_usd)


async def refresh_catalog(config: CatalogRefreshConfig | None = None) -> RefreshOutcome:
    """Refresh the runtime catalog overlay per `config`."""
    _rust_config = _to_rust_catalog_refresh_config(config) if config is not None else _rust.CatalogRefreshConfig()
    return await _rust.refresh_catalog(config=_rust_config)


def register_custom_provider(config: CustomProviderConfig) -> None:
    """Register a custom provider in the global runtime registry."""
    _rust.register_custom_provider(config=config)


def unregister_custom_provider(name: str) -> bool:
    """Remove a previously registered custom provider by name."""
    return _rust.unregister_custom_provider(name=name)


async def chat(engine: DefaultClient, req: ChatCompletionRequest) -> ChatCompletionResponse:
    """Chat."""
    _rust_req = _to_rust_chat_completion_request(req)

    if _rust_req is None:
        _rust_req = _rust.ChatCompletionRequest()
    return _from_native_chat_completion_response(await engine.chat(_rust_req))


async def chat_stream(engine: DefaultClient, model: str | ChatCompletionRequest | _rust.ChatCompletionRequest) -> AsyncIterator[ChatCompletionChunk]:
    """Chat stream."""
    if isinstance(model, str):
        req = _rust.ChatCompletionRequest(model=model)
    elif isinstance(model, _rust.ChatCompletionRequest):
        req = model
    elif isinstance(model, ChatCompletionRequest):
        req = _to_rust_chat_completion_request(model)
    else:
        raise TypeError("model must be str or a ChatCompletionRequest request")
    async for item in engine.chat_stream(req):
        yield _from_native_chat_completion_chunk(item)


async def embed(engine: DefaultClient, req: EmbeddingRequest) -> EmbeddingResponse:
    """Embed."""
    _rust_req = _to_rust_embedding_request(req)

    if _rust_req is None:
        _rust_req = _rust.EmbeddingRequest()
    return await engine.embed(_rust_req)


async def list_models(engine: DefaultClient) -> ModelsListResponse:
    """List models."""
    return _from_native_models_list_response(await engine.list_models())


async def image_generate(engine: DefaultClient, req: CreateImageRequest) -> ImagesResponse:
    """Image generate."""
    _rust_req = _to_rust_create_image_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateImageRequest()
    return _from_native_images_response(await engine.image_generate(_rust_req))


async def transcribe(engine: DefaultClient, req: CreateTranscriptionRequest) -> TranscriptionResponse:
    """Transcribe."""
    _rust_req = _to_rust_create_transcription_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateTranscriptionRequest()
    return _from_native_transcription_response(await engine.transcribe(_rust_req))


async def moderate(engine: DefaultClient, req: ModerationRequest) -> ModerationResponse:
    """Moderate."""
    _rust_req = _to_rust_moderation_request(req)

    if _rust_req is None:
        _rust_req = _rust.ModerationRequest()
    return await engine.moderate(_rust_req)


async def rerank(engine: DefaultClient, req: RerankRequest) -> RerankResponse:
    """Rerank."""
    _rust_req = _to_rust_rerank_request(req)

    if _rust_req is None:
        _rust_req = _rust.RerankRequest()
    return await engine.rerank(_rust_req)


async def search(engine: DefaultClient, req: SearchRequest) -> SearchResponse:
    """Search."""
    _rust_req = _to_rust_search_request(req)

    if _rust_req is None:
        _rust_req = _rust.SearchRequest()
    return await engine.search(_rust_req)


async def speech(engine: DefaultClient, req: CreateSpeechRequest) -> bytes:
    """Speech."""
    _rust_req = _to_rust_create_speech_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateSpeechRequest()
    return await engine.speech(_rust_req)


async def ocr(engine: DefaultClient, req: OcrRequest) -> OcrResponse:
    """Ocr."""
    _rust_req = _to_rust_ocr_request(req)

    if _rust_req is None:
        _rust_req = _rust.OcrRequest()
    return await engine.ocr(_rust_req)


async def create_file(engine: DefaultClient, req: CreateFileRequest) -> FileObject:
    """Create file."""
    _rust_req = _to_rust_create_file_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateFileRequest()
    return _from_native_file_object(await engine.create_file(_rust_req))


async def retrieve_file(engine: DefaultClient, file_id: str) -> FileObject:
    """Retrieve file."""
    return _from_native_file_object(await engine.retrieve_file(file_id))


async def delete_file(engine: DefaultClient, file_id: str) -> DeleteResponse:
    """Delete file."""
    return _from_native_delete_response(await engine.delete_file(file_id))


async def list_files(engine: DefaultClient, query: FileListQuery | None = None) -> FileListResponse:
    """List files."""
    _rust_query = _to_rust_file_list_query(query) if query is not None else None

    return _from_native_file_list_response(await engine.list_files(_rust_query))


async def file_content(engine: DefaultClient, file_id: str) -> bytes:
    """File content."""
    return await engine.file_content(file_id)


async def create_batch(engine: DefaultClient, req: CreateBatchRequest) -> BatchObject:
    """Create batch."""
    _rust_req = _to_rust_create_batch_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateBatchRequest()
    return _from_native_batch_object(await engine.create_batch(_rust_req))


async def retrieve_batch(engine: DefaultClient, batch_id: str) -> BatchObject:
    """Retrieve batch."""
    return _from_native_batch_object(await engine.retrieve_batch(batch_id))


async def list_batches(engine: DefaultClient, query: BatchListQuery | None = None) -> BatchListResponse:
    """List batches."""
    _rust_query = _to_rust_batch_list_query(query) if query is not None else None

    return _from_native_batch_list_response(await engine.list_batches(_rust_query))


async def cancel_batch(engine: DefaultClient, batch_id: str) -> BatchObject:
    """Cancel batch."""
    return _from_native_batch_object(await engine.cancel_batch(batch_id))


async def create_response(engine: DefaultClient, req: CreateResponseRequest) -> ResponseObject:
    """Create response."""
    _rust_req = _to_rust_create_response_request(req)

    if _rust_req is None:
        _rust_req = _rust.CreateResponseRequest()
    return _from_native_response_object(await engine.create_response(_rust_req))


async def retrieve_response(engine: DefaultClient, response_id: str) -> ResponseObject:
    """Retrieve response."""
    return _from_native_response_object(await engine.retrieve_response(response_id))


async def cancel_response(engine: DefaultClient, response_id: str) -> ResponseObject:
    """Cancel response."""
    return _from_native_response_object(await engine.cancel_response(response_id))
